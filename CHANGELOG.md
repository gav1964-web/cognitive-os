# Changelog

## Unreleased

- Added evidence-bound narrow-role training replay. Stable failure packets now bind the repeated failure, exact AST target, failing test source, and source digests; known KB reducers and hypothesis-only LLM candidates enter only explicitly authorized sandboxes. Five consumed CLI/pure-transform projects pass targeted and full native regression with zero generated stubs and unchanged source projects, while training caps preserve the honest `9.4/8.5/8.5/9.0/8.8/8.8` role report. Qualified method alignment no longer truncates dotted symbols, rootless Architect context no longer scans the current workspace, and generated news-scraper tests are isolated from a live local LLM gateway.
- Added a source-isolated adapter evidence layer after the conservative audit exposed a real `8.8` gap. Config-backed `logging_adapter_factory`, `logging_sink_adapter`, and `logging_record_projection` contracts now preserve allowlisted stdlib bases, fake external clients/transports, derive receiver state from AST use, and admit direct network effects only in a matching process-isolated profile. Untargeted replays on OpenSearch, systemd Journal, and Splunk handlers now produce `executable_callable` evidence and `9.7 / 9.7 / 9.8` without source changes or LLM use. An unseen Influx handler autonomously recovered from `8.8` to `9.7`; unrelated Facebook HTTP resolution and CLI/subprocess demo projects remained at `8.8` and `6.47`, confirming that the logging profiles do not broadly waive external-effect gates.
- Audited autonomous selection on three previously unused logging-adapter holdouts and removed optimistic scoring paths. Generic `handler` and substring `search` matches no longer imply resource or contract families, lifecycle-only `close`/`flush` targets are deferred, and the candidate-selection discriminator cannot force a candidate already rejected by first-slice viability. The corrected initial replay reported a conservative `8.8` floor for all three adapter-only projects instead of rewarding executable no-op wrappers, providing the failure evidence for the isolated adapter layer.
- Closed an autonomous logging-formatter capability gap on a previously unused GitLab holdout. The first Cognitive OS pass stopped at `8.8` after rejecting an effectful logger initializer; the second selected the KB-recognized formatter boundary and reproduced a missing `LogRecord.getMessage` fixture at executable acceptance; after adding a config-owned inherited-method fixture recipe, the unchanged third pass selected `SeverityPaddingFormatter.format`, produced `executable_callable` evidence with no skipped targets, and measured `9.7 / 9.7 / 9.8`. Architecture-priority promotion now also rejects direct state mutation and observed side effects, preventing `create_*` naming from elevating a database operation over a bounded transform.
- Balanced first-slice selection against convenience-helper reward hacking. Architect now receives the analyzer's complete source-backed capability set and applies config-owned architectural-significance weights before executable readiness only for complete typed contracts. Spec Writer preserves that order until measured execution failure, while the recovery loop can admit complete typed stateless service methods to source-isolated probes. This restored the architecture-useful `create_item`, `p0042/service.py:resolve`, and provider-boundary curriculum routes without weakening executable acceptance.
- Repaired the autonomous first-slice recovery loop on the `aa-inactivity` holdout. Rejected candidate evidence now retains its full structural contract, failure triggers map to canonical plugin classes, admission requires the control and treatment to match the same structural contrast, and promotion must reproduce through the ordinary untargeted route. Legacy policies without reproduction state are quarantined but may be revalidated under the same ID. Candidate search now diversifies name families and prefers receiver-free functions before dependency readiness. AST-backed constant-return hook detection prevents executable `return False` framework hooks from reward-hacking the score, while the KB `protocol_string_fallback_projection` family recognizes bounded object-to-string fallback transforms. The unchanged autonomous loop selected `inactivity/helpers.py:main_or_username`, passed executable acceptance, and improved the measured Foundation minimum from `6.47` to `9.7` (`9.7 / 9.7 / 9.8`); durable selection-policy promotion remains blocked pending independent structural evidence.
- Carried a bounded, safety-ranked Architect `source_context` candidate pool into ordinary self-improvement failure evidence, so autonomous candidate-selection shadow trials are no longer limited to the weak first slice. A live avito replay selected `project/app.py:download_file` without a manual target and improved the minimum Foundation score from 8.8 to 9.6 with no role regression.
- Stopped hypothesis validation from retraining historical matching projects; prior matches now remain regression evidence while only newly discovered matches enter holdout training.
- Added successful-contract recovery prescreening and typed-contract recovery ranking, and made candidate-selection admission synthesize a minimal sufficient discriminator instead of fragmenting an already-separated family by incidental typed-argument counts. A live independent replay selected the measured recovery target first and improved the minimum Foundation score from 5.0 to 9.7, growing the exact family to two projects without promotion.
- Made recovery discovery require addressable callables, complete input materialization evidence, bounded runtime dependencies, and a ranked pool of up to four candidates. Narrowed the KB `cli_runtime_boundary` rule to actual CLI entrypoints instead of every callable below a `cli` directory. A live Kratos replay then selected `MainAgent.finalized`, improved the minimum Foundation score from 5.0 to 9.7 with executable acceptance and no regression, and grew the homogeneous family to three training projects; independent holdout promotion is still required.
- Added untargeted holdout reproduction to candidate-selection promotion. A targeted independent holdout initially improved from 5.0 to 9.7, but the ordinary route reached only 7.5; the prematurely active policy was removed. A fresh 24-project blind field trial measured role minima of 9.2/8.33/6.47, so no stable 9.7 claim is made.
- Repaired the three exact ordinary-route failures exposed by that promotion audit without restoring the rejected policy: config-backed `datetime` and `logging.LogRecord` fixtures now make acceptance executable, pure `logging.makeLogRecord` construction no longer creates a false observability effect, and AST call evidence recognizes bounded cryptographic digest transforms through a KB contract family. Untargeted replay measured `9.7 / 9.7 / 9.8` on activity-tracker, cloudnotes, and logstruct, while the existing AWS control retained the same scores. The complete 24-project replay reduced below-target cases from 16 to 13 and raised executable callables from 7 to 9, but the corpus floor remained `6.47`; stable `9.7+` is therefore still unproven.
- Removed repeated full-source AST rescans and added bounded parsed-source and symbol indexes; the formerly timed-out `session-kit` Foundation case now completes inside the 60-second case budget.
- Scoped bounded parameter-strategy evidence to the diagnosed strategy and added a disabled-by-default, AST-derived `absolute_url` candidate that uses a local `data:` URL; unrelated instance, inheritance, and URL failures can no longer jointly authorize promotion.
- Added bounded emergent-hypothesis redirection for four or more aligned low-scoring holdouts with one exact signature, and admitted candidate-selection shadow trials for executable-sample and first-slice-reselection failures without enabling automatic discriminator promotion.
- Partitioned candidate-selection admission by exact structural discriminator and independent source projects; heterogeneous contrasts can no longer be pooled to pass the three-case promotion threshold.
- Preserved and safety-ranked the non-scoring source-backed candidate pool across evaluation target clamps, and retained executable-evidence failure classes when LLM diagnosis times out. Three homogeneous training contrasts plus an independent 7.5-to-9.6 holdout promoted the `insufficient_structural_evidence` reselection policy through a two-project regression gate.
- Allowed direct `memory_state` foundation evidence only inside the configured process-isolated acceptance boundary; external side effects remain ineligible.
- Added owner-aware contract archetype matching and an `object_pool_resource_release` KB contract, while removing the false classification of every `release_*` method as project release support.
- Replayed four previously weak stateful method targets: all four produced exact executable evidence and minimum Foundation 1-3 scores of 9.7 without source-project changes.
- Added AST-backed numeric receiver fixtures for isolated methods; replay raised `_reset_reading_order` from 7.5 to 9.7, while unrelated import, graph-fixture, and reselection failures remain explicitly below target.
- Added config-driven stdlib import fallbacks, argument-aligned required mapping fixtures, and chainable parameter protocol fixtures; the two remaining 7.5 stateful targets now reach 9.7.
- Preserved signature and structural context across first-slice viability normalization and added a `component_tree_projection` KB contract; the remaining 5.0 tree-output target now reaches 9.7, making the measured nine-target replay 9/9 at or above 9.7.
- Aligned self-improvement structural holdout targets with the Foundation 1-3 evaluation probe and exposed target drift in hypothesis validation reports.
- Expanded the memory-state holdout vocabulary in KB policy after durable search cursors proved the original query set exhausted.
- Preserved evaluation-only target clamps across Architect first-slice reselection reruns.
- Scoped frozen holdout selections to their query and page window while preserving the semantic hypothesis identity.
- Kept evaluation targets at the portfolio root instead of auto-scoping into an unrelated monorepo subtree, including syntax-damage recovery scope selection.
- Excluded vendored source trees and deletion-scheduled GitLab mirrors from adaptive structural holdouts.
- Emitted class-qualified structural targets so evaluation clamps can resolve methods unambiguously.
- Extended evaluation-only source evidence extraction to class-qualified methods.
- Restored frozen retrieval targets when revalidating historical matching holdouts.
- Added a second KB-backed memory-state search vocabulary after the prior query windows were exhausted.
- Stopped Architect red-team from treating class-qualified methods as contract type references.
- Excluded packaging setup scripts from adaptive structural targets.
- Rebuilt evaluation-only first-slice steps for the clamped target instead of retaining obligations from the original slice.
- Added autonomous hypothesis validation: Cognitive OS now builds a portable failure signature, discovers two or three unseen similar Python holdouts, trains sequentially, and returns a measured admission result before corpus verification.
- Added ordered GitLab-to-GitHub holdout discovery with provider-attempt provenance, shortage/rate-limit fallback, and cross-forge repository deduplication.
- Added diagnosis-gated holdout screening, probe reuse, and durable hypothesis-validation reports.
- Connected repeated holdout failures to typed capability-development requests.
- Tightened holdout and capability-request grouping from broad failure classes to portable structural signatures.
- Live v3 validation probed six unseen GitHub fallback projects and correctly blocked with zero signature matches instead of training two broad-class false positives.
- Added bounded multi-round holdout discovery with prior-project exclusion and per-round, per-probe, and per-training progress events.
- Live multi-round validation measured 16 unseen projects over three frozen selections and safely exhausted with zero exact signature matches, exposing the need for signature-aware query planning.
- Added a configuration-first query planner that combines portable-signature effect and output-basis vocabulary before broad failure-class fallback.
- Relaxed v5 search to interleaved signature terms plus one broad fallback, and separated normal empty rounds from complete provider-chain failures.
- Fixed multi-round provider pagination so successive rounds inspect disjoint page windows instead of repeatedly scanning pages 1-2.
- Separated adaptive-holdout provider eligibility from release-corpus policy, allowing a bounded GitHub star-floor override without weakening evaluation or admission.
- Live v7 found the first exact signature holdout (`FletX`, 8.8); its portable metadata expanded the v8 query vocabulary with `global state` and restored `event handler` within a six-query cap.
- Added bounded durable reuse of exact prior probes, with current-evaluator revalidation and artifact-boundary checks, so search-policy evolution no longer forgets accumulated evidence.
- Expanded v9 discovery vocabulary with portable state-management, global-state, event-driven, state-machine, and cache metadata while leaving evaluation and admission unchanged.
- Live v9 revalidated three durable probes and measured six new projects; only FletX retained the exact staged signature, so the autonomous gate withheld training and promotion after bounded search instead of learning from six structurally unrelated repositories.
- Added v10 two-stage hypothesis retrieval: forge metadata freezes a wider candidate pool, a bounded configuration-driven AST screen freezes a portable-signature shortlist, and only the shortlist reaches the full role probe; structural screening remains explicitly non-scoring retrieval evidence.
- Live v10 screened 18 new projects down to six full probes and found a semantically equivalent explicit-`None` holdout; v11 normalized both void-output syntaxes, revalidated two durable holdouts, withheld promotion after two flat 8.8 training results, and autonomously requested a `candidate_selection_discriminator_plugin` from three-project evidence.
- Added Plugin Foundry capability resolution and a bounded candidate-selection discriminator over ranked, reselection, and ADR targets; fixed shadow treatments so measured application is distinct from durable promotion.
- Added required fresh-holdout discovery, compatible effect-superset signatures, fresh-first training, bounded parallel cloning, and per-clone failure isolation.
- Added measured-effect admission, pairwise structural effect synthesis, normal SpecWriter preflight/Architect reselection integration, and transactional regression/refinement/rollback for learned selection policies.
- Live v12-v16 improved `report-generator` and FletX from 8.8 to 9.6, and `python-memoization` plus unseen `Jarvis-v13` from 8.8 to 9.7. A broad policy exposed an `hfos` regression and was removed; v17 then probed eight further unseen projects and blocked with no new matching holdout, so no policy or readiness increase was claimed.
- Added v18 structured diagnosis envelopes, effect-plus-output query composition, explainable exact/compatible/contrast signature decisions, and recovery yield/discrimination metrics; contrast cases remain forbidden from training.
- Added v19 normalized-family query vocabulary and forbidden high-risk additional effects after live v18 evidence showed an empty composed query set and a known filesystem-write regression misclassified as compatible.
- Added v20 durable semantic search cursors across plan-version changes; live v19 replay rejected the known regression and reached `1.0 / 1.0` retrieval metrics on four prior matches but remained blocked with no fresh project.
- Added v21 recovery of a missing redundant diagnosis class from an otherwise complete portable baseline signature; live v20 resumed at pages 7-12 but confirmed zero fresh forge candidates.
- Added v22 per-query durable cursors, bounded query rotation, provider-dialect translation, broader adaptive-only eligibility, and new portable state vocabulary. Live replay supplied 16 new full probes and one near contrast but no fresh exact match, isolating prescreen-to-first-slice alignment as the next blocker.
- Added post-training candidate-selection admission so the best measured challenger can be validated and promoted by the registered plugin cycle without manual project repair; corpus regression still rolls promotion back.
- Separated adaptive hypothesis holdouts from large blind release corpora and made GitLab provider failures explicit blockers rather than negative hypothesis evidence.
- Added a minimum-based downstream field trial for Implementer, Task Tree Builder, Programmer Executor, Tester, and Reviewer, including executable-callable, skipped-target, strategy, sandbox-candidate, and verifier evidence.
- Hardened executable acceptance against project-owned `SystemExit` and unguarded top-level infinite loops; hazardous modules now fail a sample or use source isolation instead of terminating or hanging the corpus run.
- Reused the shared Python compatibility parser in source-contract analysis and patch synthesis, preserving Python 2 callable evidence without mutating analyzed source.
- Completed GitLab blind iteration 11 on 40 unseen projects: blind Foundation floor `6.47`, post-treatment role minima `9.7 / 9.7 / 9.8`, with 30 Python-owned projects and 10 controlled out-of-scope projects.
- Added seeded L4.5 generated corpus support (`--generated-corpus-size`, `--seed`) and verified a deterministic 200-case field trial.
- Added PromptBoundaryClassification, expanded the L4.5 semantic benchmark to 22 cases, added deterministic-vs-model comparison reports, and ran a live `GigaChat-Pro` propose-only trial with replay-backed results.
- Added the L4/L4.5 semantic-loop toolkit: `SemanticEvidencePack`, `SemanticProposalReplay`, `L4DecisionTable`, L4.5 model quality modes, deterministic semantic benchmark and acceptance gate.
- Added `L4SemanticValidationResult` as the explicit L4.0 gate for L4.5 proposals; Stage 2 template backlog items are now created only after accepted L4 validation.
- Promoted `csv_sort_cli` from L4.5 template backlog candidate to admitted deterministic Stage 2 template with project-scoped tests.
- Added Stage 2 template admission gate and optional model-backed L4.5 semantic proposal mode with deterministic fallback.
- Added deterministic L4.5 semantic runner, `SemanticHypothesisProposal` validation, and Stage 2 template backlog items for bounded unsupported prompts.
- Added explicit L4.5 `SemanticHypothesisRequest` contracts for bounded semantic escalation from L4.0 back into deterministic gates.
- Routed Stage 2 prompt-to-product through L4.0 `CognitiveControlPlaneDecision`, so `PromptAdequacyGate` is now an API input to deterministic build/clarify/stop transitions.
- Added L4.0 Cognitive Control Plane v0.1 with deterministic artifact promotion, role transition, semantic-escalation policy and crystallization backlog.
- Extended Tester executable acceptance to v0.3 with project-specific invocation for simple `file.py:function` targets, output-shape checks and malformed-input rejection.
- Added Tester executable acceptance v0.2: TestPlan obligations now generate a pytest scaffold, Programmer Executor records ExecutableAcceptanceResult, and Reviewer conformance blocks failed executable acceptance.
- Added deterministic L3.5 selection diagnostics, Tester executable acceptance obligations, Reviewer conformance checks, L4 raw-model quality status, and documented controlled LLM fallback after deterministic failure.
- Documented the LLM Replacement Policy: LLMs are bounded hypothesis sources inside a verifiable engineering machine, while contracts, deterministic hardening, conformance checks and tests decide promotion to artifacts or execution.
- Switched the explicit external L4 gateway default from a provider routing alias to `GigaChat-Pro`; all L4 CLIs now share the same environment-overridable profile.
- Portable MVP acceptance is now self-contained: external L4 and machine-local project corpora are explicit opt-in field trials, memory evidence is seeded deterministically, and queue summaries expose packet counts without leaking full results.
- Documentation now distinguishes the sandbox-only general Programmer Executor from the explicit reviewed specialized patch-apply gate.
- Positioning and evaluation-plan documents clarifying that Cognitive OS is a contract-driven intent-to-engineering pipeline and verification harness, not a competing chat/workspace agent UI.
- Documentation now states that role artifacts are APIs between layers, not prose-only reports.
- Evaluation corpus scaffold with task template, metrics contract and validation CLI.
- Initial 6-task evaluation corpus and summary CLI for direct-agent vs Cognitive OS comparison.
- Architecture hypothesis track documenting role, registry, artifact, layering, Stage 3, memory, Foundry and debug-loop claims plus four seed evaluation tasks.
- First completed evaluation run for `task01_project5_improvement_plan`, recording a direct-agent win on human-readable analysis breadth and Cognitive OS strengths in artifact traceability/source safety.
- Architecture analysis document now supports current ProjectMapReport answer keys (`1_scope`, `2_execution`), eliminating `n/a` purpose/entrypoint sections in the first evaluation rerun.
- Completed `task02_map_project_analysis`, recording a direct-agent win and a Cognitive OS backlog item for stronger packaged-copy/source-strata filtering.
- Project map reports now exclude packaged-copy paths from active execution evidence, avoid usage/not-included README text as project purpose, and add human-readable improvement recommendations.
- Architecture analysis documents now include a target architecture sketch; `task02_map_project_analysis` was rerun to `no_clear_difference` after the project-analysis fixes.
- Layer 3.5 now has a contract facade in `runtime/spinal_planner.py` for IntentPacket -> validated Pipeline DSL -> MotorPlanPacket/SignalPacket, plus interrupt adaptation and quality checks.
- User goals now pass through mandatory `runtime/goal_runtime.py` coordination; L2 emits correlated execution/interrupt packets and recovery returns through a bounded L3.5 adaptation loop.
- Async execution and durable workers now use the same spinal recovery controller; queue results persist packet and adaptation traces, and async fallback no longer leaks the original exception into node outputs.
- Sync and async executors now re-enter L3.5 when a retry or fallback also fails, producing a second interrupt and a blocked signal when the adaptation budget is exhausted.
- L3.5 deterministic benchmark covers known chains, graph planning, safe blocking, retry, fallback and escalation, with contract and latency metrics.
- Contract Registry now catalogs role artifacts as producer/consumer APIs in addition to capabilities and packet routes.
- `goal_run.py` emits ASCII-safe valid JSON on Windows even when analyzed source contains characters unsupported by the console encoding.
- Stage 3 `Prompt -> Verified Product Slice` contract with `ProductSliceSpec`, architecture decision, task graph, verification summary, CLI and acceptance gate.
- Stage 3 product-slice review fields for requirements, task dependencies, documentation review, scenario verification, bounded product debug-loop plan and inferred input/output lifecycle.
- Executable Stage 3 product debug loop with documentation/scenario failure analysis, bounded package-local rework, project-scoped verification rerun, tester refresh and acceptance probe.
- Stage 3 product debug-loop repair for FastAPI API contract drift and a 3-case product-slice benchmark.
- Stage 3 product debug-loop repairs for CLI UX drift and README/API mismatch, with acceptance probes.
- Stage 3 product-slice benchmark expanded to 8 supported product prompts, with prompt intake and negative-edge evidence hardening.
- Stage 3 product-scenario probe for non-repairable CLI core behavior drift, producing a controlled `needs_rework` boundary instead of blind repair.
- Focused LLM migration analyzer for finding local proxy assumptions and producing direct-provider migration plans.
- GigaChat sandbox patch generator for building reviewed, tested direct-provider migration packages without touching the source project.
- Sandbox patch review/apply gate with diff, risk summary, verification checks, explicit approval flag and source backup.
- Standalone Stage 2 debug-loop probe for damaged FastAPI packages and controlled rework verification.
- CLI input/output contract repair for generated file-processing packages.
- Negative and edge-case test repair for empty text input and malformed JSONL fixtures.

## v0.1.0-mvp - 2026-07-08

Initial public research preview.

- Layered Cognitive OS runtime with capability registry, contract registry, queue, checkpoints, interrupts, and worker pool.
- Capability Foundry flow for spec generation, sandbox candidate build, tests, and dry-run promotion.
- Project Analyzer, Architect, SpecWriter, Implementer Planner, Tester, Reviewer, and sandbox Programmer Executor role artifacts.
- Stage 2 `Prompt -> Verified System Package` path for bounded CLI/FastAPI packages.
- Stage 2 support for JSONL log-filter CLI, text-stats CLI, FastAPI CSV aggregator, and FastAPI key/value CRUD service.
- Bounded Stage 2 debug loop for allowlisted deterministic repairs.
- Human-readable architecture analysis documents generated by role pipelines.
- MVP acceptance suite and project analyzer benchmark fixtures.
- GitHub Actions CI for pytest and MVP acceptance.
