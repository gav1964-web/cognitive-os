# COGNITIVE_OS_TECHNICAL_BASELINE.md
**Инженерная спецификация и требования к MVP**

**Project-native hardening note (2026-08-29).** Assertion-causal traversal считает test helpers, external calls и dynamic observers непрозрачными: вложенные production calls сохраняются как `opaque_observer_descendant` для диагностики, но не дают patch authority. Rich подтвердил необходимость gate: snapshot через test-local `render(card)` падал с Pygments `2.21.0`, тогда как project-locked `2.19.2` прошёл исходный nodeid; `make_test_card` не является доказанным defect target. Все bounded subprocess запускаются в отдельной Windows process group или POSIX session, а timeout завершает process tree; `pyproject-api` после hardening вернул два bounded timeout вместо удержания inherited pipes дочерними Python-процессами. Два дополнительных current-head pure-transform batches охватили 19 проектов и дали 12 clean, 7 blockers, 0 qualified failures, 0 source changes. Поэтому следующий discovery gate использует source-complete pre-fix upstream commits с исходным regression test и read-only fix reference; искусственное повреждение source и ослабление authority запрещены.

**Historical pre-fix validation note (2026-08-29).** Parameterized pytest nodeids теперь сохраняют embedded spaces; intake project stratum переносится в recognition и reassessment как explicit evidence. Независимый Deepmerge holdout доказал failure-specific reducer для различения falsy primitives и null/sized-empty значений: targeted test и полный suite прошли в sandbox, исходный corpus не изменился. Узкий pure-transform ledger достиг `3/3`; это основание для `9.7+` только в данном lane, не для CLI или широких strata.

### 1. Спецификация контрактов (Уровень 1)
* **Код не является единственным носителем знаний:** Требования, архитектура, контракты, критерии приемки, политики, role artifacts и evidence trails являются самостоятельными authority-артефактами системы. Если они описаны достаточно полно, реализацию можно воспроизводить, проверять и улучшать по мере развития генераторов кода без изменения самой архитектуры. Source code является важной материализацией этих знаний, но не единственным источником истины.
* **Single Responsibility:** Один плагин — одна capability с одним внешним entrypoint. Запрещен вызов плагина из плагина.
* **Plugin Isolation:** Плагин не знает о существовании других плагинов, не импортирует их код, не вызывает их entrypoint и не принимает решений о pipeline routing. Композиция capabilities выполняется только runtime-уровнем через Pipeline DSL и Capability Registry.
* **Plugin Layout:** Плагин является каталогом с кодом, manifest, схемами и контрактными тестами. Минимальная структура: `plugin.json`, `schemas/input.json`, `schemas/output.json`, `src/`, `tests/`.
* **Правило 400 строк:** Физический размер одного Python-файла внутри плагина <= 400 строк. Все, что больше — декомпозируется на модули внутри `src/`, но внешний entrypoint capability остается один. Нарушение правила блокирует загрузку capability. То же ограничение действует как repo-level gate для Python source в `runtime/`, `tools/`, `plugins/`, `tests/` и checked-in generated sample projects; машинные артефакты (`artifacts/`, `generated/candidates/`) не являются source authority. Крупные embedded templates должны жить как template assets или каталоги файлов, а не как гигантские Python string payload.
* **Интерфейс:** Строгая валидация вход/выход через `JSON Schema` и typed runtime artifacts. Схемы хранятся как отдельные артефакты плагина. Никакого сквозного проброса всего стейта.

Project-to-capability extraction проходит dependency extraction policy до sandbox build. Self-contained functions допускаются; stdlib dependencies из allowlist могут быть импортированы в generated wrapper; unresolved local/domain calls, unsafe runtime boundaries (`subprocess`, network, secrets/database/write-side effects) и instance/class-bound methods без object adapter policy блокируют конкретный candidate, пока не появится явная политика bundling локальных project modules, process isolation или адаптации объектов. Грубая analyzer-метка `filesystem` сама по себе не блокирует candidate, потому что может означать read-only context; реальные ограничения filesystem фиксируются в side-effect manifest и downstream gates. Foundry обязан пройти ranked candidate list: если первый candidate не самодостаточен, он фиксируется в `skipped_candidates` с `dependency_policy_blocked`, после чего система пробует следующий ranked candidate. Proposal полностью блокируется только если все подходящие candidates нарушают dependency policy. Foundry не имеет права молча копировать соседние модули проекта или превращать несамодостаточную функцию в полурабочий plugin.

MVP capability library должна содержать не только demo HTML pipeline, но и базовые атомарные file/text/html/json/pdf/project transforms, чтобы planner мог строить полезные DAG без генерации нового кода на каждый шаг. Первые новые capabilities, проведенные через Foundry generate/validate/candidate/promote цикл, фиксируются как `translate_text` и `parse_pdf`. `parse_pdf` допускает optional production backend (`pypdf`) при наличии в окружении, но обязан иметь deterministic builtin fallback без обязательной внешней зависимости. File-conversion pack включает `markdown_to_text`, `markdown_to_rtf`, `csv_to_spreadsheet` и `spreadsheet_to_csv`; modern `.xlsx`/`.csv` paths исполняются как deterministic capabilities, а legacy `.xls`/`.xlwt`-зависимые paths должны останавливаться через Knowledge Gap preflight, если optional backend отсутствует. Project-analysis pack начинается с intake chain: `scan_project_tree` выполняет bounded read-only обход дерева с исключением noise directories, лимитами глубины/количества файлов, расширениями, notable files и признаками truncation; `detect_project_stack` извлекает языки, framework signals, dependency files, entrypoints, scripts и large artifacts, но исключает packaged-copy каталоги вида `*_install_package` из entrypoints/scripts; `read_many_files` безопасно читает выбранные текстовые файлы с лимитами и поддерживает `auto_discover` для README/config/start/API files без чтения noise directories вроде reports/artifacts/scratch; `extract_python_structure` строит AST summary по Python-файлам, imports, functions/classes, Flask/FastAPI routes, central nodes, broad functions, pure transform candidates, type-hint contracts, schema-like fields, import graph hints, test surface counters, explicit raise/try/except hints и external dependency categories. Pure transform candidate не считается чистым, если он напрямую вызывает локальную функцию с side effects: такой wrapper относится к orchestration/boundary анализу, а не к pure capability. `extract_runtime_commands` извлекает команды запуска/установки/пересборки из scripts; `project_map_report` собирает итоговый Markdown/JSON отчет с summary, risks и answer-oriented sections по scope, execution, capabilities, contracts/data, errors/state/reproducibility и runtime extraction readiness: data lifecycle, mixed responsibilities, hidden orchestrators, long-lived state, idempotency risks, quarantine candidates, process boundaries, contract-test strategy, resume/reuse plan, source strata и minimal extraction plan. Для первого extraction plan действует core-first policy: тесты, `conftest.py`, scratch/examples/docs/generated/tools, packaged copies, legacy analyzer branches и build/package helper scripts учитываются как контекст или шум, но не предлагаются как первые reusable core capabilities. `source_strata` обязан явно разделять `active_core`, `legacy_noise`, `context_only` и `packaged_copy`; ArchitectSkill использует это разделение как evidence при выборе first extraction candidates.

`project_map_report` может использовать `project_archetype_rule` из `knowledge/architecture_patterns/project_archetypes.json` как декларативный профиль назначения проекта, если rule содержит `purpose_summary`, `scenario_summary`, `input_summary` и `output_summary`. Это не дает rule права выбирать implementation target, но позволяет держать распознавание домена в KB. Quality gate обязан предупреждать о конфликте между `ProjectMapReport.answers.1_scope.main_task` и `domain_profile`, чтобы неверная raw-классификация не скрывалась последующими ADR/Spec исправлениями.

Если входной root классифицирован как `dirty_portfolio`, snapshot-mix или damaged source tree, foundation pipeline не имеет права строить `ArchitectureDecisionRecord` и `TechnicalSpec` по всему дереву. Project Analyzer обязан выпустить `ScopeSelectionReport`: список candidate roots, evidence по шуму/архивам/generated/test_workspace, `selection_confidence`, blocked downstream artifacts и действие `choose_active_root_before_role_pipeline`. При нескольких кандидатах `preferred_candidate` остается пустым, а `selection_confidence=ambiguous`; выбор active root должен быть явным входом человека или отдельного проверяемого gate, а не догадкой Architect/SpecWriter.

Явный выбор active root фиксируется отдельным `ActiveRootDecision`: `portfolio_root`, `selected_root`, `selected_relative_path`, источник решения и policy "downstream roles may run only on selected_root". После такого решения Project Analyzer, Architect и SpecWriter работают только с выбранным подкаталогом, сохраняя исходный портфель как context/reference, но не смешивая его с active source evidence. Допускается `auto_safe_scope_selector`, если `ScopeSelectionReport` показывает один доминирующий Python/root-package кандидат (`src`, `lib`, root package или явно core/sdk root) с сильным score-gap и без disfavored roots вроде `tests`, `examples`, `docs`, `scripts`, `benchmarks`; такой auto-decision обязан быть записан в `ProjectMapReport.source_health.active_root_decision` и `noise_exclusion_decision`. Auto-selection может делать bounded nested выбор (`repo -> src -> package`), но не должен уходить в случайные subpackages, если текущий root уже является root package.

Метрика field trial разделяет safety и readiness. `blocked_ok` является корректным controlled block, но не равен полноценному `ArchitectureDecisionRecord -> TechnicalSpec` handoff и не может засчитываться как `10/10`. `RoleFoundationFieldTrialReport` обязан публиковать `readiness_min_score`, `readiness_avg_score`, `usable_handoff_rate` и `controlled_block_rate`; для readiness controlled block получает ограниченный score, чтобы массовые безопасные блокировки не маскировались под зрелость ролей.

Python foundation field trial обязан иметь primary-language gate. Проект, где top-level workspace явно принадлежит другой экосистеме (например, Cargo/Rust workspace с `crates/`, большим числом `.rs` файлов и только embedded Python assets/templates/tests), должен попадать в `out_of_scope_for_python_foundation` / `unsupported_primary_language_for_python_foundation`. Такой кейс остается в отчете и влияет на coverage, но не входит в scored readiness для Python-ролей. Это защищает метрику от двух ошибок: насильно анализировать не-Python primary project и считать корректный отказ провалом `Project Analyzer -> Architect -> SpecWriter`.

### 2. Capability Registry (Уровень 2.5)
Каждый плагин на диске обязан иметь паспорт в системном реестре, содержащий:
* `Determinism Grade`: Класс надежности от Grade A (чистая математика) до Grade E (внешняя нестабильная среда/LLM).
* `Side-effect Manifest`: Декларация разрешений на доступ к файловой системе (`write_scoped`, `read_only`), сети (`allowlist`, `none`) и системным секретам.
* `Lifecycle Status`: Текущее состояние capability: `active`, `degraded`, `quarantined`, `rebuilding`, `retired`.

Registry обязан предоставлять scoring выбора capabilities. Минимальный порядок предпочтения: `active` выше `degraded`, более высокий `Determinism Grade` выше низкого, меньшие side effects выше широких разрешений. `quarantined`, `rebuilding` и `retired` не участвуют в выборе для новых pipeline.

Registry quality metrics дополняют scoring после жестких lifecycle/contract ограничений: failure rate, success count и latency используются как операционный сигнал, но не могут сделать quarantined capability исполнимой.

Registry selection report должен объяснять порядок выбора capabilities через status, determinism grade, side effects, quality и latency.

### 2.1 Contract Registry

Capability Registry отвечает на вопрос "какие инструменты существуют и в каком они lifecycle-состоянии". Contract Registry отвечает на другой вопрос: "какие входы, выходы, side effects и межслойные маршруты разрешены runtime прямо сейчас".

Contract Registry реализуется в `runtime/contract_registry.py` как производный источник истины поверх plugin manifests, JSON Schemas, межслойных packet routes и API-контрактов ролевых артефактов. Он не дублирует `registry/capabilities.json` вручную и не становится вторым mutable registry. `GoalSpec`, `ProjectMapReport`, `ArchitectureDecisionRecord`, `TechnicalSpec`, `ImplementationPlan`, `TestPlan`, `PatchPackage`, `TestResult` и `ReviewFindings` публикуются в том же read-only catalog с producer/consumer и обязательными полями. Причина: один mutable реестр быстро приведет к drift, а производный contract catalog можно пересобрать и проверить в любой момент.

Минимальное enforcement-правило: Pipeline DSL валиден только если каждая node ссылается на capability с существующим executable contract (`active` или `degraded`), object input/output schemas и допустимым side-effect manifest. `quarantined`, `rebuilding` и `retired` contracts не исполняются даже если plugin-каталог физически существует. Межслойные packets валидируются по route/payload contract до сохранения как replay/debug artifact.

Role artifact contracts являются тем же API-слоем. `ArchitectureDecisionRecord` не считается готовым для SpecWriter только по наличию `chosen_option`: обязательны tradeoff-ranked `architecture_options`, объясненные `rejected_options` со score delta и deferred-until condition, bounded source-backed `first_slice_contract` с selection policy, actionable risk model с impact/mitigation/evidence, `source_context`, source-linked `traceability`, actionable `spec_writer_brief` и forbidden-action enforcement. `first_slice_contract` обязан иметь evidence density: выбранные targets должны совпадать с `source_context`, `capability_model` или `traceability`, а `architecture_synthesis.project_profile` не может оставаться пустым generic placeholder при наличии source-backed extraction facts. Если `ProjectMapReport` еще не содержит явного `architecture_synthesis`, `ArchitectureDecisionRecord` строит conservative fallback из `minimal_extraction_plan`, entrypoints и dataflow evidence вместо выпуска пустого первого среза. `runtime/architect_red_team.py` выпускает `ArchitectRedTeamReport` с verdict `ready_for_spec_writer` или `return_to_architect`; foundation pipeline обязан учитывать этот verdict как gate. `TechnicalSpec` не считается готовым для Implementer только по наличию текста: обязательны `source_evidence`, `extraction_contract`, `work_plan_contract`, `interface_contracts`, `acceptance_criteria` и `traceability_table`. `extraction_contract.ranked_candidates` обязан содержать source и причины выбора минимум для верхних кандидатов; выбранный candidate обязан быть связан с `source_evidence` или ranked source, а `semantic_quality.status=poor` блокирует готовность. `runtime/role_artifact_quality.py` проверяет, что I/O shape не остается слабым `Any -> Any`, а side-effecting target имеет validation/idempotency/process/retry gate. Дополнительно `runtime/spec_writer_red_team.py` выпускает `SpecWriterRedTeamReport` с verdict `ready_for_implementer` или `return_to_spec_writer`; foundation pipeline обязан учитывать этот verdict как gate. Для доменных slices вроде `llm_auto_repair_loop` допустим domain `contract_family`, но тогда он обязан раскрывать typed contract shapes, validation gates и failure modes. Семантика first-slice targets и domain `contract_family` является configuration-first: `config/semantic_target_profiles.json` хранит exact/prefix/contains symbol matchers, path markers, profile exclusions, score/ranking adjustments, benign runtime-boundary markers, typed I/O contracts, side-effect policy, validation gates и failure modes; `runtime/semantic_target_profiles.py`, `runtime/target_quality.py`, `runtime/role_spec_writer_ranking.py` и `runtime/technical_spec_builder.py` только интерпретируют эти записи. Новые target families должны добавляться как профиль+тест, а не как новая ветка role/domain logic в Python, кроме случаев расширения самого универсального matcher/validator.

Контур развития существующего Python-проекта является отдельным typed API над анализом и ролями: `ProjectDevelopmentDiagnosis -> ProjectDevelopmentOptionPortfolio -> ProjectDevelopmentDecision -> ProjectDevelopmentOutcomeContract -> optional role chain -> optional sandbox experiment -> reassessment -> validated memory`. Diagnosis обязан ссылаться на evidence из `ProjectMapReport`, `ProjectRecognitionDecision` и, при наличии, предыдущего full-chain case; policy `config/project_development_policy.json` ранжирует проблему и варианты по severity, confidence, value, cost, risk и reversibility. Unknown recognition, damaged source и dirty portfolio направляются в research до ролевой реализации. Решение с authority `ProjectDevelopmentDecision` ограничивает first-slice reselection списком `allowed_targets`: роли могут выбрать другой target только внутри evidence выбранной проблемы, а исчерпание кандидатов приводит к `needs_replanning`. Sandbox execution допускается только для allowlisted deterministic issue reducers и готового `ImplementationDelta`; Config Doctor требует, чтобы каждый reducer совпадал с зарегистрированным `patch_synthesis_policy.recipes.*.operation_kind`. Если разрешено несколько reducers, каждый проверяется в отдельном sandbox candidate и выполнение допускается только при одном `prepared` результате; ноль или несколько совпадений блокируют patch. Исходный проект хешируется до и после выполнения и не меняется. Outcome не считается достигнутым по наличию артефактов или запуску тестов: обязательны воспроизведение baseline evidence, уменьшение выбранной проблемы, targeted acceptance, bounded executor verification и сохранение source scope. `ProjectDevelopmentValidatedMemory` получает authority только после validated reassessment; неуспешный или неизвестный случай остается `not_promoted`. Для `mixed_responsibility` development authority имеют только `extract_json_dumps_helper` для одной inline JSON-сериализации непосредственно в `write_text`, `extract_json_loads_helper` для одного inline parse вокруг `read_text`, `extract_splitlines_helper` для одного `.splitlines()` site и `extract_append_mapping_helper` для одного dict-literal mapping в `append` внутри простого цикла, когда mapping зависит только от переменной цикла, а accumulator является единственным возвращаемым значением функции. Loop-local conditional field подтверждён report `project_development_20260828T070645103118Z.json`, а дополнительная внешняя fallback-переменная в contrast `project_development_20260828T070723270674Z.json` блокирует extraction. Соседняя форма `record = {...}; accumulator.append(record)` подтверждена report `project_development_20260828T072338364901Z.json` только при одном store и одном load временной переменной; reuse contrast `project_development_20260828T072433082449Z.json` блокируется. Config-backed включительный предел `maximum_mapping_fields=12` подтверждён report `project_development_20260828T080840365016Z.json`, а 13-field contrast `project_development_20260828T080915669163Z.json` блокируется. Nearest loop обязан быть синхронным `for` без enclosing `for` или `async for`: simple-loop report `project_development_20260828T083944554908Z.json` валидирован, nested-loop contrast `project_development_20260828T084003587919Z.json` блокируется. Множественные sites, cross-reducer ambiguity и более широкие helper shapes fail closed. `filesystem_read` консервативно классифицируется как risk profile `filesystem`, а string-параметр с точным именем `path` получает ephemeral readable path fixture при executable acceptance. Source apply и автоматическое продвижение в общую KB запрещены.

Generated-function stub admission является обязательным post-synthesis gate. AST delta сравнивает только затронутые Python-файлы оригинала и sandbox и блокирует новую функцию либо регрессию существующей функции, если её исполняемое тело сводится к `pass`, `...`, `raise NotImplementedError` или `return NotImplemented` (необязательный docstring не маскирует marker). Уже существующие interface stubs не считаются нарушением этого delta. При нарушении `ProjectDevelopmentExperiment.checks.no_generated_function_stubs=false`, project-native replay не запускается, experiment получает `failed`, reassessment остаётся `not_validated`, а validated memory и KB promotion запрещены независимо от остальных зелёных тестов.

Project-native environment preparation сохраняет installed-metadata semantics без сети и поддерживает distribution-bound interpreter profiles. Требуемые major/minor обязаны разрешиться в конкретный executable и version evidence; silent fallback запрещён. После неуспешного editable install допускается один local non-editable `pip install .` с `--no-deps --no-build-isolation`; он создаёт entry points в одноразовом probe venv, а parent source остаётся первым через controlled `PYTHONPATH`. Project dependency overlay предшествует shared tool overlay, pytest external plugin autoload отключён; исключения требуют distribution-bound `-p` allowlist. Git worktree pointers, `[project].name` из pyproject и короткий digest basetemp поддерживаются явно. Flake8 под Python `3.12.9` прошёл targeted `1 passed` и regression `465 passed`. Isort добавил независимый `trailing_backslash_index_contract`: targeted `1 passed`, regression `548 passed, 5 skipped`, stub admission и source invariant. Target-scoped admission может снять только проектный `stateful` risk для повторяемой единственной функции без прямых внешних эффектов; остальные запрещённые риски не снимаются. Матрица `role_project_type_evaluation_20260830T104433802113Z.json` подтверждает CLI `3/3`, transaction `project_native_repair_promotion_20260830T104614251822Z.json` активировала три узких CLI-паттерна.

Project-native `failure_repair` отделён от capability extraction. Право на патч возникает только из одинакового canonical failure signature в двух независимых sandbox copies и единственного production target. Target связывается traceback frame или консервативным assertion-causal AST slice: matcher идёт от `assert` назад через локальные присваивания и допускает только единственный вызов production-кода проекта. Test helpers, external dependencies, unresolved instance calls и несвязанный setup не получают production authority. Переменная длительность pytest удалена из signature, oversized Windows nodeid ограничен и классифицируется как environment blocker. Nodeid нормализуется token-aware: разделители пути меняются только до первого `::`, escaped parameter payload сохраняется. Windows copies выполняются под коротким `.nfi/<run>/<project>/rN/p`, что устранило ложный bytecode failure `installer`; `GIT_CEILING_DIRECTORIES` не позволяет build backend случайно обнаруживать родительский Git-root workspace. После timeout полного suite runner использует шесть file shards; timeout одного file shard допускает bounded collect до 50 000 nodeid/4 МБ при максимуме 2048 символов на один nodeid и до 16 refined shards. `click` probe провёл 30 163 nodeid через `refined_shards_after_timeout`. Probe venv сохраняется даже при editable-install fallback; controlled `.pth` ставит sandbox source и dependency overlay в начало `sys.path` дочерних процессов. Это сняло `typer`/`shellingham` blocker и довело обе копии до одинакового formatting assertion. Causal report `project_native_failure_intake_20260829T101438223637Z.json` исключил `runner.invoke` как unresolved instance call и `normalize_rich_output` как test observer, поэтому статус остался `reproducible_unbound_failure`. Явный dependency install hint и любой `ModuleNotFoundError` являются environment blockers даже при production frame; это закрыло ложную authority для optional extras в `validators` и `markdown-it-py`. Успешные shard outcomes могут кэшироваться только по project/overlay/interpreter/platform/pytest/nodeid digest; failures не кэшируются, patch verification принудительно отключает cache. Реальный Click shard сократился с `5.751s` до cache hit `0.001s`. Operational evidence staged в `project_native_execution_profiles.json`. Дополнительный holdout из 31 независимого pure-transform проекта дал 19 clean baselines, 12 environment/incomplete-source blockers и 0 qualified failures. Новый source-complete holdout из 8 внешних библиотек дал 7 clean baselines и один честный Python-version blocker. Для объявленных dynamic-version backends intake ограниченно переносит `.git` (до 5000 файлов/20 МБ), благодаря чему Toolz собирается как `1.1.0` и проходит 186 тестов дважды; если default discovery пуст и единственным suite является корневой `tests.py`, он выбирается явно и записывается в `test_targets`, что дало jsonpointer 23 теста дважды. Clean и blocked результаты не увеличивают transformation count, поэтому gate остаётся `2/3`. `FailureBackedChangeRequest` сохраняет failing nodeids и failure kind; SpecWriter выпускает target-bound repair contract, Implementer ограничен failure-specific reducer, Tester повторяет исходный nodeid и полный native suite. Generated acceptance остаётся дополнительным сигналом и не заменяет native oracle. Validated repair cases: `platformdirs`, reducer `fallback_missing_registry_to_env`, targeted `1 passed`, regression `895 passed, 79 skipped`; `marshmallow`, reducer `normalize_timestamp_range_error`, targeted `1 passed`, regression `1184 passed`. В обоих случаях source apply `false`.

Failed sandbox experiment теперь завершает не только `ProjectDevelopmentReassessment`, но и typed `ProjectDevelopmentExecutionFeedback`. `config/project_development_policy.json.feedback_policy` классифицирует matcher uncertainty (`no_unique_development_helper_extraction`, ambiguity и `*_pattern_not_proven`) как `research` с маршрутом `Researcher -> Architect`; prepared patch с failed verification возвращается Architect как `needs_replanning`; неизвестная или admission-причина даёт `controlled_stop`. Feedback переносит `patch_reason`, reducer attempts и failed checks, сохраняет allowed targets и запрещает automatic retry, scope expansion и source apply. Report `project_development_20260828T090705601690Z.json` подтверждает `research_required` и `not_promoted`, а contrast `project_development_20260828T090747254502Z.json` подтверждает `not_required/completed` для validated experiment.

Для feedback с decision `research` запускается read-only continuation `ProjectDevelopmentResearchHypothesis -> ProjectDevelopmentArchitectFeedbackDecision -> ProjectDevelopmentFeedbackContinuation`. Researcher разрешает только исходный target, извлекает bounded AST facts и выбирает hypothesis kind из закрытого policy-списка. Architect после source identity, confidence и no-retry gates принимает только `replan`, `research_more` или `controlled_stop`. `replan` меняет top-level status на `needs_replanning`, но не создаёт ImplementationPlan, Developer handoff или executor retry. Report `project_development_20260828T093240287559Z.json` подтверждает `nested_loop_mapping_boundary` с confidence `0.93`, `architect.replan`, source changes `false`, executor rerun `false` и memory `not_promoted`; validated contrast `project_development_20260828T093325940127Z.json` оставляет continuation `not_required`.

Решение `replan` материализуется в `ProjectDevelopmentReplanRevision` как planning-only revision исходных `ProjectDevelopmentDecision` и `ProjectDevelopmentOutcomeContract`. Revision gate требует authority-baselines, тот же target, неизменный selected option, полный исходный набор required checks, increment ровно `+1` и закрытые запреты на Developer, Executor и source changes. Нарушение любого gate даёт revision `blocked` и общий `controlled_stop`. Report `project_development_20260828T100823826409Z.json` подтверждает revision `2`, `execution_authorized=false`, source digest invariant и memory `not_promoted`; validated contrast `project_development_20260828T100905579561Z.json` содержит `replan_revision=null`.

Planning revision компилируется в `ProjectDevelopmentBoundedExperimentProposal` и проверяется отдельным `ProjectDevelopmentBoundedExperimentAdmission`. Admission допускает один primary target и один immutable allowlisted contrast source, максимум три evidence requirements, нулевой execution/source-change budget и закрытый read-only allowlist действий. Он не запускает внешнюю role chain, Developer или Executor. Contrast path обязан оставаться внутри workspace, а digest, ожидаемые AST facts и validated report identity проверяются до принятия evidence.

После planning admission embedded read-only Researcher автоматически выпускает `ProjectDevelopmentBoundedExperimentEvidence`, а Architect — `ProjectDevelopmentArchitectEvidenceDecision`. Nested-loop contrast теперь является парой source AST: nested negative и validated simple-loop positive оба source-backed, оба digest остаются неизменными. Это даёт `bounded_implementation_evidence=true`, но `kb_promotion_evidence=false`; `evidence_accepted` ведёт только к `human_review_bounded_implementation_candidate`. Report `project_development_20260828T113702427711Z.json` подтверждает paired evidence и memory `not_promoted`; validated contrast `project_development_20260828T113733891395Z.json` оставляет оба артефакта `null`.

Переход paired evidence в candidate проходит через `ProjectDevelopmentImplementationApprovalRequest -> ProjectDevelopmentHumanApprovalDecision -> ProjectDevelopmentImplementationApprovalValidation -> ProjectDevelopmentBoundedImplementationCandidate`. Request привязан к canonical SHA-256 evidence и proposal ID; approval принимается только от authority `human` с явным `approve | reject` и reason. Digest/request replay, неверный authority и reject не создают candidate. Даже успешный candidate имеет status `approved_not_executable` и отдельный future gate. Report `project_development_20260828T115407964184Z.json` фиксирует реальный `pending` request без human decision; approved report намеренно отсутствует. Validated contrast `project_development_20260828T115440020888Z.json` не создаёт approval artifacts.

После approved candidate создаётся `ProjectDevelopmentImplementationDesignRequest`, который проходит `ProjectDevelopmentImplementationDesignAdmission`. Явно поданный `ProjectDevelopmentImplementationDesign` проверяется отдельным `ProjectDevelopmentImplementationDesignValidation`: candidate/proposal/evidence/target и source SHA должны совпадать, четыре design outputs обязательны, а запрещённые ключи отклоняются рекурсивно. Source patch, ImplementationPlan, executor task, automatic role invocation, execution и memory promotion запрещены. Current report `project_development_20260831T080044961164Z.json` фиксирует human-approved design как `accepted_not_executable` с canonical design digest; следующий gate — отдельная implementation authorization.

Implementation authorization реализована отдельной цепочкой `ProjectDevelopmentImplementationAuthorizationRequest -> ProjectDevelopmentImplementationAuthorizationDecision -> ProjectDevelopmentImplementationAuthorizationValidation -> ProjectDevelopmentAuthorizedImplementationResult`. Request связывает canonical design digest, evidence digest, target source SHA и execution-policy digest. Разрешение действительно только для одного sandbox target с targeted и full regression verification; source apply и memory promotion закрыты. Первый реальный report `authorized_implementation_20260831T081035446426Z.json` имеет `verified_in_sandbox`: targeted replay passed, pytest-socket `103 passed, 9 skipped`, stub gate passed, изменён только approved sandbox file, исходный project digest неизменён. Это supervised transformation `1/3`; autonomous transformation и KB promotion не заявляются.

Второй независимый supervised transfer подтверждён на `urllib3.LocationParseError`. Upstream test проверял только concrete type и пропускал порчу `location` и двойной message prefix. Исправленный matcher исключает классы с локально унаследованным reconstruction hook, поэтому isort больше не считается кандидатом. Design-bound recipe `reuse_direct_assignments` допускает только точные уже существующие присваивания constructor input в `self` state. Report `authorized_implementation_20260831T110602578287Z.json` имеет `verified_in_sandbox`: semantic round trip passed, targeted `17 passed`, bounded regression `19 passed`, generated `_version.py` удалён как доказанный build artifact, итоговый Python scope равен одному target file. Ledger теперь `2/3`; source apply, autonomous execution и KB promotion остаются запрещены.

Третий supervised transfer закрыт на `python-redmine.UnknownError` из существующего exposed corpus. Direct semantic probe доказал, что pickle replay сохранял type и `status_code`, но превращал message в двойной prefix. Report `authorized_implementation_20260831T115835185733Z.json` имеет `verified_in_sandbox`: semantic round trip passed, targeted replay `1 passed`, bounded regression `405 passed`, no generated stubs, original source unchanged. Runner также научился убирать sandbox-only `build/lib` от локальной wheel-сборки перед scope gate. Ledger достиг `3/3`; это закрывает supervised threshold, но autonomous execution, source apply и KB promotion по-прежнему запрещены.

Exception-pickle promotion readiness теперь оценивается отдельным gate, а не текстовым выводом из ledger. `ExceptionPicklePromotionReadiness` проверяет independent projects/targets, verified reports, semantic/native/stub/source invariants, audit availability, holdout boundary и валидность autonomous reports. Текущий report `exception_pickle_promotion_readiness_20260831T124021424040Z.json` имеет `promotion_review_allowed=true` и `autonomous_activation_allowed=true` при `autonomous_verified_count=1`, но `kb_promotion_allowed=false`: direct promotion из readiness запрещён.

Independent holdout transaction, autonomous shadow run и independent evaluator review добавлены как отдельные gates. После explicit transaction `exception_pickle_promotion_transaction_20260831T132156422950Z.json` активирован KB catalog `exception_pickle_reconstruction_patterns.json`; promotion прошёл `20` focused regression tests и Config Doctor `45/45`. Boundary interpreter потребляет active catalog как overlay: matching boundary получает `status=active` и `validated_active` operator authority. Pattern остаётся sandbox-only by default, без source apply и без automatic runtime mutation. `ExceptionPickleActiveApplicationTrial` теперь проверяет post-promotion reuse отдельно: current ledger has `applied=178`, `blocked=414`, records blocker taxonomy, and proves sandbox-only successful semantic replay across 178 independent holdout targets including latest `mercadopago__sdk-python / InvalidWebhookSignatureError`, `eddyzzl__marvis-risk-agent / IllegalTransition`, three CPython `tarfile` destination/link errors, `not-sekiun__consortium / AgentGeneratorBuildStepOverridesFinalMethodError`, `abhinavsingh__proxy-py / ProxyConnectionFailed`, `dj-bolt__django-bolt / ComponentNameCollisionError`, `pybotters__pybotters / OutOfBoundsError`, two `shy3130` tickflow `CapabilityDenied` targets, two `nats-io__nats-py` client errors, `nerevu__riko / PipelineStateError`, `withceleste__celeste-python / UnsupportedParameterError`, `reloadware__reloadium / RedefinedVarError`, two `benoitc__gunicorn` stash errors, `vonage__vonage-python-sdk / PartialFailureError`, `software-mansion__starknet-py / OutOfBoundsError`, `reloadware__reloadium / NoTypeError`, `supabase__supabase-py / StorageApiError`, `disnakedev__disnake / MaxConcurrencyReached`, `mosaik__mosaik / InvalidNextStepTypeError`, `nextcord__nextcord / MaxConcurrencyReached`, two `rapptz__discord-py` command errors, `treeverse__dvc / BaselineMismatchError`, and `rluna-iot__home-assistant / RequirementsNotFound`, `pact-foundation__pact-python / InteractionVerificationError`, `rapptz__discord-py / Discord command invoke`, `psevensas__djangochannelsgraphqlws / GraphQL WS response`, Alembic range/head errors, JMComic base error, two Redis HTTP errors, and `treeverse__dvc / OutputDuplicationError`, while keeping original source unchanged. `ExceptionPickleBlockerIntelligence` now adds the planning layer: latest report `exception_pickle_blocker_intelligence_20260903T065159200878Z.json` classifies the latest-blocker frontier at 146 cases; sample-supported readmission frontier is 0, class-state contract research is separated at 6 cases, and import/dependency isolation remains the priority lane at 25 cases. Import-isolation summary splits those 25 into dependency unavailable 9, import failed 5, attribute-base/metaclass risk 10 and one dependency-heavy sample-supported case, with direct-file replay preferred for 14; missing-import kinds are external dependency 11, project-local dependency 2 and stdlib symbol compat 2. Top recurrent missing import is now `librt` at 2; `authlib`, one `yarl` target and one `voluptuous` target were closed, while report-only probes marked `aiohttp`, `async_timeout`, `sqlalchemy`, `app` and `common` as diagnosis-required rather than direct-write clusters. Source-aware sliced-string/rendered-body materializer corrected `line[:100]` and REST `body` replay without converting numeric `lineno` fields; `topics`, `domain`, `service`, `bucket`, string-membership, `.lower()`, source-backed attribute-object samples, `.value`, bounded nested data objects, recursive nested named-object replay, CPython `frozendict` compatibility, `types.GenericAlias` replay shim and suffix-based `*_filepath` string samples are supported. The replay subprocess now injects sandbox import paths only after stdlib bootstrap, so local files such as `src/types.py` cannot shadow stdlib modules before compatibility shims are installed. The patcher now admits stored-state constructors whose single `super().__init__` call has zero or multiple arguments, plus direct-state keyword-only constructor cases through an explicit state-only `BaseException.__new__` reducer flag; semantic replay normalizes self-referential exception args only when state still matches, never target-stubs stdlib imports, allows same-package absolute imports in the direct-file stub contour, and blocks readmission for target files with imported attribute-base class definitions. Active application keeps `maximum_replay_blockers`, `--only-readmission-frontier`, `--only-import-isolation-frontier`, `--import-isolation-missing-kind`, `--import-isolation-batch-profile dependency_heavy_direct_file`, `--import-isolation-cluster`, `--maximum-accepted-applications`, `--readmission-subtype`, precheck-aware ordering, patchability ordering, dependency-light replay ordering, per-project static-patch/replay blocker budgets, and true no-ledger-update dry-run unless CLI `--write` is present, plus `--write-report` for persisted probe evidence without ledger mutation. The latest project-local exact-target writes accepted `langchain-ai__langchain / SSRFBlockedError`, `shy3130__tick-stock-panel / JobCancelledError`, and `astronomer__agents / NotFoundError` after a report-only project-local batch probe. Report-only probes are now persisted with `--write-report` without ledger mutation; saved probes marked `aiohttp`, `async_timeout`, `sqlalchemy`, `app` and `common` as failed direct-file probes, while planner report `exception_pickle_import_cluster_planner_20260903T065218069738Z.json` marks the next `unknown:attribute_base_metaclass_risk` cluster as `not_batch_ready` with `class_state_contract_research_before_replay`. Failed-probe analyzer `exception_pickle_failed_probe_analyzer_20260903T065213416880Z.json` classifies 2 latest failed probes and now recommends `constructor_state_contract_research`; behavior-contrast, reprobe and replay-capture repair lanes were closed by this cycle. Object-contract audit/admission `exception_pickle_object_contract_audit_20260902T095949529132Z.json` / `exception_pickle_object_contract_admission_20260902T100010126192Z.json` closed the source-backed object-materializer tail in the current BI. Optional L4.5 LLM classification is advisory-only and cannot override deterministic source evidence or grant source apply / KB promotion.

Знания project-development boundary вынесены из Python-ветвлений и policy в staged KB: `project_development_boundary_profiles.json` хранит match predicates, hypothesis output, confidence, strategy и requirement rules, а `project_development_source_contrasts.json` хранит digest-pinned source identity, expected AST facts и provenance. `project_development_boundary_interpreter.py` является отдельным узким интерпретатором этого формата; существующие `project_interpreter` и `role_artifact_interpreter` решают другие задачи. Допустимы только закрытые операторы `eq`, `gt`, `suffix` и композиции `all`/`any`; неизвестный оператор не совпадает, а не расширяет authority. Шесть frozen source cases, включая два blind и шесть lineages, проходят characterization, но профиль остаётся staged: accuracy одного корпуса не заменяет независимые reports, promotion transaction и no-role-regression gate.

### 2.2 Skill Registry

Skill Registry отвечает за профессиональные когнитивные роли верхних уровней, а не за исполняемые инструменты. `registry/skills.json` описывает role skill id, слой, входные/выходные артефакты, entrypoint и forbidden actions. Он не заменяет Capability Registry и не участвует в выборе runtime plugins.

Усиление ролей оценивается по матрице `role x project_stratum`, а не одной общей оценкой роли. Policy находится в `config/role_project_type_evaluation.json`; promotion требует worst-case score не ниже `9.7`, минимум пять проектов, два blind-кейса и два независимых отчета. Для Implementer, Tester и Reviewer дополнительно требуются минимум три project-native transformations: derived mutation benchmark может сохранить measured score, но получает `score_scope=bounded_transformation_contracts` и не закрывает maturity. Неизмеренный тип проекта не наследует зрелость роли из другого типа. Три оси публикуются раздельно: `maturity` показывает изученность role×type области, `readiness` - прохождение ролью независимых сквозных gates, `confidence` - надежность конкретного вывода в конкретном запуске. Текущий baseline `artifacts/field_trials/role_project_type_evaluation_20260829T035206805890Z.json` содержит 642 наблюдения из 107 отчетов: 79 измеренных ячеек, 38 mature, 40 usable, одна weak, 24 not applicable и две unmeasured. Это намеренная рекалибровка authority, а не регрессия исполнения.

Явная последовательность развития начинается с `cli_local_tool` и `library_pure_transform` и использует promotion target `9.7` для всех обязательных ролей. Foundation-часть narrow lane закрыта. Downstream-часть остаётся открытой: `library_pure_transform` имеет две из трёх требуемых native transformations (`Implementer 9.8`, `Tester 10.0`, `Reviewer 9.8`), `cli_local_tool` имеет `0/3`; обе области сохраняют maturity `usable`. Следующий gate - ещё один независимый pure-transform delta нового subtype и три CLI delta; broad deferred lane возобновляется только после него.

Historical narrow deficit `SpecWriter x cli_local_tool = 9.6` воспроизведён на `dbcli__pgcli`. Исправлен общий executable-acceptance дефект: synthetic `receiver_state` method fixture больше не считается обязательным пользовательским input для strict-negative, но явный required `receiver_state` остаётся строгим. Policy-backed floor `9.7` применяется только при совпадении downstream target с выбранным candidate, `status=passed`, `source_code_changes=false`, strong candidate quality и полном прохождении SpecWriter semantic checks. Multi-project reports `role_foundation_min_field_trial_20260828T131723371616Z.json` и `role_foundation_min_field_trial_20260828T131859214887Z.json` подтверждены независимыми executable runs на `psf__black` (`role_foundation_min_field_trial_20260828T132440866583Z.json`) и `python-attrs__cattrs` (`role_foundation_min_field_trial_20260828T132331581925Z.json`). Все четыре отчета сохраняют source changes `0`; итоговая promotion authority принадлежит агрегированной role×type матрице, а не single-project calibration.

Интерпретатор развития проекта отделяет наблюдение от authority. `SourceIncompletenessEvidence` AST/token-aware классифицирует sole `pass`, `raise NotImplementedError`, intent comments и exception handlers как `intentional_marker_type`, `intentional_interface_boundary`, `intentional_exception_suppression`, `declared_future_work` или `ambiguous_stub`. Ни один из этих классов сам по себе не создаёт issue; нужен target-bound `failing_contract_test`, `executable_acceptance_failure` или `explicit_user_failure`. Аналогично high/medium risk требует scanner, runtime или failing-test authority. Внешний Comfy CLI negative control `project_development_20260828T173504992818Z.json` сохраняет TODO, auth/oauth и structural signals как observations, но выдаёт `issue_count=0`, `controlled_stop` и не запускает цепочку ролей. При entrypoint override классификация отдельно публикует `effective_project_identity` и `project_archetype_scope`, поэтому внутренний parser capability больше не выглядит вторым типом CLI-продукта.

Текущая последовательность hardening определяется свежими сквозными failures, а не ростом уже насыщенной матрицы. Независимый executable transfer `pilot_transfer_trial_20260827T085921925543Z.json` прошёл 2/2 проекта. Два callable rework-паттерна первого operational batch закрыты обобщёнными AST samples и подтверждены диагностически, но новый пяти-owner corpus `pilot_run_batch_20260827T110808502633Z.json` дал 0/5 first-pass: четыре eligible target требуют nominal/protocol fixtures, один filesystem/subprocess case корректно заблокирован профилем. Human review подтвердил `rework=4`, `stop=1`; telemetry `pilot_telemetry_20260827T112019244368Z.json` имеет пять валидных digest-bound решений, pending `0`, handoff loss `0`, source changes `0`. Поэтому следующий порядок ролей: (1) Architect/SpecWriter учитывают проверяемую executability при ранжировании source-backed candidates; (2) Programmer Executor расширяет только обобщённые nominal/protocol materializers; (3) исправления подтверждаются на новом unseen operational corpus; (4) лишь затем продолжаются semantic-boundary gates и возможное расширение отдельных risk/type профилей. Автоматический approval и source apply запрещены.

Для создания нового проекта действует отдельный greenfield-контур: `UserPrompt -> ProductArchitectureRecord -> ProductTechnicalSpec`. Его нельзя подменять synthetic `ProjectMapReport` и extraction-first логикой существующего проекта. `ProductArchitectureRecord` обязан описывать назначение продукта, сценарии, компоненты, интерфейсы, data model/lifecycle, external boundaries, `ResearchHints`, `ArchitectureOptions`, chosen/rejected decisions, security policy, state/replay policy, риски, non-goals и open questions. `ResearchHints` являются advisory-запросами на проверку фактов: они подсказывают, какие official docs, API, библиотеки или похожие проекты надо проверить перед реализацией, но не считаются ground truth и не означают, что внешний research уже выполнен. `ArchitectureOptions` обязаны фиксировать хотя бы один выбранный и один отклоненный вариант с tradeoffs; для OpenVPN это защищает систему от опасного пути "реализовать протокол самому" и направляет к management/profile service вокруг OpenVPN/CA adapters. Greenfield planning является configuration-first: `config/greenfield_architecture_patterns.json` хранит pattern records с prompt markers, product summary, components, contracts, research hints, architecture options, risks, open questions, acceptance focus и constraints, а Python-код только интерпретирует эти записи. Добавление новой greenfield-предметной области не должно требовать role-specific source edits. `generic_product` не считается успешной архитектурной классификацией: если конкретный pattern не найден, contour обязан остановиться до SpecWriter, записать `semantic_gap.reason_code=greenfield_pattern_missing`, сформировать `SemanticHypothesisRequest` для L4.5, вернуть proposal в L4.0 validation и при отсутствии проверяемого existing route оформить `DeveloperImprovementRequest` для Codex/human доработки. Реальный L4.5 вызов включается только явно (`--use-l45-llm`/профиль окружения); при ошибке provider используется deterministic proposal, но факт semantic miss не маскируется под готовое ТЗ. Open questions являются управляющим контрактом, а не только разделом документа. По умолчанию Architect запускается в режиме `continue_with_assumptions`: он идет дальше независимо от вопросов, но переносит их в архитектурный документ, TechnicalSpec manual review, риски и handoff. Альтернативный режим `ask_user_before_spec` останавливает контур после `ProductArchitectureRecord`, возвращает `status=needs_clarification` и формирует `clarification_prompt` для уточнения исходного запроса пользователем; `ProductTechnicalSpec` в этом режиме не строится до ответа пользователя. `tools/verified_system_package.py --planning-only` подключает этот же Architect/SpecWriter contour к обычному пользовательскому prompt flow без запуска Implementer. `ProductTechnicalSpec` обязан содержать primary contract, chosen architecture option, research hints, component contracts, requirements, error model, acceptance criteria, verification strategy и implementation handoff. Human-readable greenfield документы являются reading surface поверх typed artifacts и тоже входят в качество: архитектурный документ обязан содержать архитектурное решение, research hints, архитектурные варианты, первый полезный срез, допущения до уточнения, план проверки и открытые вопросы; ТЗ обязано содержать решение для реализации, выбранный архитектурный вариант, исследовательские подсказки, главный контракт, data lifecycle, стратегию проверки и вопросы для ревью. `GreenfieldArtifactQualityReport` является deterministic gate для этих артефактов: он проверяет non-generic pattern, specific summary, component contracts, interfaces, data model/lifecycle, research hints, chosen/rejected architecture options, mitigated risks, open questions, requirements, error model, acceptance criteria, verification strategy, bounded handoff, clean forbidden actions и наличие обязательных human-doc sections без untranslated intake questions. Этот контур остается ролью Architect/SpecWriter: он не пишет код, не запускает Foundry, не меняет registry и не исполняет pipeline.

`Project Analyzer` формирует `ProjectMapReport` как фактический read-only API для следующих ролей. Отчет обязан содержать не только summary, но и `source_health`: `status`, `project_shape`, `syntax_error_count/samples`, `inaccessible_count/samples`, generated/duplicated run signals, packaged/snapshot-copy signals, artifact-noise signals, entrypoint/dependency counts, truncation flag и recommendation. `project_shape=dirty_portfolio` означает, что под одним root смешаны разные проекты/runs/generated copies; такой root нельзя трактовать как обычный single-project extraction target без выбора конкретного подкаталога. Snapshot-каталоги вида `*_YYYYMMDD`, packaged copies, archives, logs, caches и `.env` считаются evidence для sanitation policy: вложенные snapshot sources должны попадать в `source_strata.packaged_copy`, а не в `active_core`. `source_health.status=damaged` или `project_shape=dirty_portfolio` не должен маскироваться под успешную архитектурную готовность: Architect/SpecWriter могут строить reading artifacts, но `ArchitectRedTeamReport` обязан вернуть `return_to_architect`, пока нет явного `active_root_decision`. Обычный noisy single-project без snapshot-mixing может пройти handoff только с warning о `noise_exclusion_decision`; warning снижает confidence, но не подменяет архитектурный анализ. Остальная часть `ProjectMapReport` содержит source-backed ответы по назначению/границам проекта, входам/выходам, entrypoints, execution path, core/adapters/tests/noise areas, reusable capabilities, broad/mixed responsibility functions, weak contract zones, data structures, likely errors, state/reproducibility, data lifecycle, process/quarantine/idempotency risks, resume/reuse policy, source strata и minimal extraction plan. Project Analyzer не вызывает LLM и не делает архитектурных выводов без evidence.

L4 fact digest для project analysis переносит route evidence (`route`, methods, path, function) как факт первого класса. Архитектурные KB rules не должны угадывать LLM gateway по общим словам `provider`, `proxy` или `FastAPI`: `llm_gateway_service` требует явного chat/completions evidence (`/v1/chat/completions`, `/chat/completions`, `chat_completions` или `_handle_chat_request`), а более общие web/proxy/framework rules обязаны уступать при таких маркерах. Это защищает Architect от ложной классификации обычного provider metadata API как LLM gateway и от классификации настоящего OpenAI-compatible gateway как generic web/proxy/workflow.

Project factual question answering строится как configuration-first interpreter. Пользовательский вопрос к уже собранным фактам проекта сначала преобразуется в `ProjectFactQuerySpec`: target, filters, sort, limit, output fields и answer key. Python-код выполняет только общий executor этого контракта над `ProjectMapReport`/tree evidence; конкретные формулировки вроде "файлы больше N", "имя содержит X", "топ по размеру" описываются в `config/project_fact_query_patterns.json`, а не добавляются отдельными ветками в source. Symbol facts по AST-структуре описываются в `config/project_symbol_query_patterns.json`: функции/классы/методы рассматриваются как queryable symbols, например `function.name contains token -> files/functions evidence`; для точного запроса по имени и пути допускается bounded source snippet по `line/end_line`, но только из уже найденного AST symbol. Call-level symbol questions допускают explanation по конкретному вызову внутри конкретной функции: runtime считает реальные occurrences в bounded source snippet, возвращает строки/evidence и использует declarative call hints для краткого назначения и причин повторения. Текстовые факты из исходников/README/config извлекаются через `config/project_text_fact_extractors.json`: extractor задает file selection, regex, scoring и output field, а код остается универсальным scanner/extractor. Import/effect факты описываются в `config/project_import_fact_extractors.json`: answer key, import roots и side-effect labels находятся в конфиге, а не в source. Более сложные source-code факты описываются в `config/project_code_fact_analyzers.json`: analyzer задает candidate paths, line signals, cases, derived answers и evidence selection, а код только исполняет этот declarative contract. Если вопрос нельзя выразить существующими query patterns, symbol patterns, text/import extractors или code fact analyzers, L4/L4.5 должен оформить semantic gap или гипотезу нового декларативного pattern, а не требовать one-off правку deterministic plugin.

`ArchitectSkill`: `ProjectMapReport + GoalStatement -> ArchitectureDecisionRecord`. ArchitectSkill живет на Уровне 4, может формировать subsystem boundaries, capability model, risks, non-goals, open questions, traceability, architecture options, chosen option, rejected options и brief для SpecWriterSkill, но не имеет права писать код, менять Capability Registry, исполнять pipeline или выполнять promote. Усиленный ADR обязан включать subsystem boundaries с inputs/outputs, data lifecycle, state model, external boundaries, quality attributes и SpecWriter brief с contract targets; LLM/L4.5 может быть только источником гипотез, а не authority поверх фактов ProjectMapReport. Доменный archetype выбирается через декларативную KB: multi-agent/A2A/consensus/orchestration projects должны получать `multi_agent_orchestration_runtime` и first slice вокруг orchestrator/consensus/protocol contracts, а `llm_gateway_service` допускается только при evidence реального chat/completions request-forward-normalize path.

Следующая роль `spec_writer` преобразует `ArchitectureDecisionRecord -> TechnicalSpec`: bounded scope, requirements, acceptance criteria, preserved non-goals, traceability table и implementation handoff. Усиленный TechnicalSpec является API для downstream-исполнения: он обязан содержать interface contracts, extraction contract, ranked candidates, data lifecycle, state/replay policy, error model, verification strategy, source-linked acceptance criteria и bounded handoff. SpecWriter не расширяет scope за пределы ADR и не подменяет архитектурный выбор новым target.

`TechnicalSpec.extraction_contract.semantic_quality` является отдельным advisory API. Он не заменяет contract validation и не объявляет ground truth, но отделяет "формально связанный контракт" от "архитектурно полезного первого среза". Сигнал обязан учитывать ranked candidates, source evidence и selection reason, усиливать parser/factory/resolver/normalizer/query-like bounded contracts и помечать meta-infrastructure, runtime/API boundary, trivial accessor/value helper и support/helper targets как `suspicious` или `poor`. Человекочитаемое ТЗ обязано выводить этот сигнал рядом с выбранным extraction contract.

SpecWriter ranking дополнительно выполняет negative source-evidence gate для first-slice candidates. Если snippet содержит высокоуверенные необъявленные имена, например связку `pipe/output` в ad-hoc evaluator, candidate получает явный штраф и reason `snippet references unresolved names`; он не должен вытеснять более надежную generation/postprocessing/service boundary. Этот gate является сигналом выбора контракта, а не полноценной статической проверкой всего проекта.

Роль `implementer` в текущем MVP является `Implementer Planner` и преобразует `TechnicalSpec -> ImplementationPlan` для существующего проекта или `ProductTechnicalSpec -> ImplementationPlan` для greenfield-проекта. Для extraction/refactor режима target обязан быть привязан к `TechnicalSpec.extraction_contract`: patch scope, expected files, implementation units, change plan, quality gates, verification commands, rollback plan и acceptance mapping. Для greenfield режима target обязан иметь `implementation_target.mode=greenfield_project`, `greenfield_project_plan`, bounded `expected_files`, component-to-file map, implementation units по `ProductTechnicalSpec.component_contracts`, adapter boundaries, fixture-first verification, conservative dependency policy и sandbox-first `ExecutorHandoff`. Она не пишет код сама; ее задача — подготовить bounded work package для отдельного исполнения.

`ImplementationPlan` обязан содержать executor-facing API, а не только человеческое описание: `ImplementationBlueprint` фиксирует target, source contract, edit strategy, quality gate ids и acceptance binding; `PatchIntent` фиксирует sandbox-first режим, allowed files/write scope, verification commands и запрет source apply по умолчанию; `ExecutorHandoff` связывает план с `tools/apply_implementation_plan.py`, входными артефактами `TechnicalSpec + ImplementationPlan + TestPlan` и ожидаемыми выходами `PatchPackage + TestResult + Reviewer handoff`. Эти поля являются машинными контрактами между Implementer Planner и programmer executor. Декларативные stop-rules внутри `ExecutorHandoff.blocked_if` не считаются фактическими сбоями L4.5 escalation; L4.0 оценивает только структурные status/reason/code сигналы.

Роль `programmer_executor` является отдельной ролью: `ImplementationPlan -> isolated patch package -> verification run -> TestResult -> Reviewer handoff`. В MVP она готова только в sandbox/no-source-edit режиме: `runtime/programmer_executor.py` и `tools/apply_implementation_plan.py` создают `PatchPackage`, копируют writable files в `source_snapshot`, запускают allowlisted verification commands, сохраняют `TestResult` и передают его Reviewer через role pipeline `--run-executor`. `runtime/programmer_patch_synthesizer.py` поддерживает узкие deterministic coding patterns для простых top-level Python targets: required-input guards, contract-backed transforms и recovery extraction recipes. Текущие recovery recipes выделяют `normalize_record`, `serialize_json`, `parse_json` и `split_lines`; они применяются только при единственной доказанной AST-границе и fail-closed при неоднозначности. Acceptance запускается против patched sandbox, а исходный проект остается неизменным. Executor также обязан выполнять встроенную project-scoped verification по `ImplementationPlan.expected_files`: для Python-файлов запускается `py_compile` внутри execution project/sandbox, чтобы field trial видел проверку конкретного изменяемого файла, а не только root-level smoke. Общий executor блокирует прямое изменение source project даже при явном `--apply-source`. Специализированный recovery apply требует Tester differential pass, normal Architect re-entry, exact patch digest, source precondition и явный human approval; failed post-apply verification запускает rollback. Это узкий reviewed patch applier, а не универсальное разрешение на автономные source edits. Роль нельзя подменять `Implementer Planner`, Foundry dry-run или ручной работой Codex внутри сессии.

Если исчерпан список semantically safe first-slice candidates, обычный implementation path сохраняет `blocked_no_safe_candidate` и запускает условный recovery-контур `Reviewer -> Researcher -> Architect -> Developer -> Tester -> Architect`. `runtime/recovery_pattern_audit.py` разделяет syntactic extraction и semantic/security boundary research, а `runtime/recovery_boundary_research.py` формирует bounded hypotheses для request mapping, network response decoding, dynamic subprocess commands и structured streams. Для semantic boundaries `developer_handoff_allowed=false`, пока source review, transport/process invariants, independent holdout и success/failure differential не пройдут Architect gate. Наличие Researcher hypothesis не является разрешением на патч.

Для развития настоящей роли программиста вводится Rebuild Trial Loop: `source project -> Project Analyzer -> ProjectRebuildSpec -> generated project_x -> comparison report -> tooling/role backlog`. Первый полигон — `map -> map_x`. Цель этого контура не копировать проект побайтно, а строить архитектурно эквивалентный минимальный проект по evidence: entrypoints, routes, data artifacts, core capabilities, scenarios и runtime boundaries. `runtime/project_rebuild.py` и `tools/project_rebuild_trial.py` создают scaffold и сразу оценивают соответствие. `runtime/project_rebuild_app_templates.py` выбирает MVP-режим по evidence: `map_flask`, `api_fastapi` или `tooling_cli`, но behavioral layer не должен быть набором частных сравнителей по framework. `runtime/project_rebuild_behavior.py` строит общий behavioral probe plan из `ProjectRebuildSpec`: safe HTTP probes для read-only routes и capability-manifest probe для tooling/library проектов. Технологические различия скрываются в adapters, которые приводят Flask/FastAPI/etc. к единой форме response shape. До генерации scaffold система может снять source response blueprints по safe probes и использовать их как контракт формы ответа generated проекта. `runtime/project_rebuild_samples.py` переносит в rebuild только тонкие read-only срезы source JSON, а не synthetic-only fixtures. `runtime/project_rebuild_ui.py` выполняет статический UI smoke только там, где generated app имеет UI surface. Низкий или пограничный score является полезным сигналом для развития аналитика, архитектора, спецификатора, программиста, тестера и reviewer; система не должна считать stub scaffold полноценной реализацией.

Роль `tester` преобразует `TechnicalSpec + ImplementationPlan -> TestPlan`: contract test matrix, acceptance tests, executable acceptance obligations, negative tests, smoke checklist, regression risks и reproducibility notes. Она не запускает тесты сама; execution остается за явным runtime/tooling шагом. Tester обязан удерживать `test_target` равным `ImplementationPlan.implementation_target`, сохранять `writable_scope` как единственный изменяемый контур, переносить `evidence_scope` в read-only context и ставить filesystem/network/provider/LLM/browser/subprocess границы в fake/fixture-by-default режим. Качество роли проверяется отдельным `Tester Curriculum Local-3/External-3` через `runtime/tester_curriculum.py` и `tools/tester_curriculum.py`; GitHub tester probe применяет Python primary-language gate, считает non-Python-primary workspaces `out_of_scope` и измеряет source changes по before/after git snapshot. Summary публикует `avg_score`, `worst_case_score`, `ready_threshold=0.92` и `ready_by_worst_case`, а переход дальше оценивается по худшему проекту, не по среднему.

Роль `reviewer` преобразует `TechnicalSpec + ImplementationPlan + TestPlan + optional TestResult -> ReviewFindings`: findings, conformance checks, coverage assessment, risk assessment, contract violations, architecture drift, rework tasks и recommendation. Reviewer обязан отличать residual risks от blocking rework: `approve_with_risks` документирует риски без автоматического `BoundedReworkPlan`, а `request_rework` возникает при failed conformance, high findings, contract violations, architecture drift или failed TestResult/ExecutableAcceptanceResult. Качество роли проверяется `Reviewer Curriculum Local-3/External-3` через `runtime/reviewer_curriculum.py` и `tools/reviewer_curriculum.py`; GitHub reviewer probe применяет Python primary-language gate, считает non-Python-primary workspaces `out_of_scope`, требует usable binding и измеряет source changes по before/after git snapshot. Summary публикует `avg_score`, `worst_case_score`, `ready_threshold=0.92` и `ready_by_worst_case`. Ролевые skill-тесты в MVP являются deterministic artifact-contract tests и не обязаны обращаться к LLM. Если LLM backend будет добавлен в роль позже, его выход обязан проходить через тот же typed contract и deterministic fallback.

Сквозной gate верхнего контура представлен `tools/github_full_chain_probe.py`: `ProjectMapReport -> ArchitectureDecisionRecord -> TechnicalSpec -> ImplementationPlan -> TestPlan -> ReviewFindings`. Он оценивает handoff целиком, а не отдельные роли: typed artifact chain, ADR traceability/source context, source-backed TechnicalSpec target, неизменность target через Implementer/Tester/Reviewer, usable binding, bounded writable scope, contract/negative tests, Reviewer conformance, отсутствие contract violations, architecture drift, forbidden targets, source edits, registry changes и LLM-вызовов. Summary обязан публиковать `worst_case_score`, `ready_threshold=0.92` и `ready_by_worst_case`; успешный full-chain status возможен только по худшему scored project. Этот gate не доказывает runtime execution или качество сгенерированного кода; он является обязательной pre-execution проверкой перед развитием Programmer Executor.

Ближайший стабильный gate верхнего контура — Role Foundation Pipeline: `ProjectMapReport -> ArchitectureDecisionRecord -> TechnicalSpec`. Он реализован в `runtime/role_foundation_pipeline.py` и `tools/role_foundation_run.py`, прогоняется минимум на одном benchmark-проекте и не запускает Implementer/Tester/Reviewer, Foundry, source edits или registry changes. Gate считается успешным, если ProjectMapReport содержит structured answers, data lifecycle и runtime extraction readiness; ADR содержит chosen option, tradeoff-ranked architecture options, subsystem I/O boundaries, data/state models, traceability и source context; а TechnicalSpec содержит requirements, interface contracts, source-linked acceptance criteria, traceability table, `source_evidence`, `extraction_contract`, error model и state/replay policy. При `write=True` контур пишет два человекочитаемых Markdown-документа: `human_documents.architecture_analysis` и `human_documents.technical_spec`; они являются reading surface поверх typed JSON artifacts, а не заменой контрактов. `runtime/human_document_quality.py` выпускает `HumanRoleDocumentQualityReport` и проверяет, что документы сохраняют машинную цепочку `ProjectMapReport -> ArchitectureDecisionRecord -> TechnicalSpec`, русскоязычную читаемость, evidence/traceability, validation gates, acceptance criteria и handoff. `extraction_contract` выбирает ranked best-first candidate, содержит `ranked_candidates`, `candidate_score` и `selection_reason`; выбранный `candidate` должен быть первой строкой ранжирования. Benchmark corpus задает `expected_best_extraction_candidate`, а foundation field trial проверяет `candidate_match_score`. Авто-выбор active root допускается только при сильном разрыве между кандидатами или очевидном named-package root; после выбора downstream-роли работают только внутри selected root, а исходный portfolio root сохраняется как audit context. Parser drift из-за более нового Python-синтаксиса в selected root фиксируется как `source_health` risk, но сам по себе не блокирует архитектурный handoff, если нет inaccessible paths или packaged-copy сигналов. Foundation readiness считается по худшему scored project; `blocked_ok` является controlled stop, но не считается 9.2+/10 readiness.

Полный Role Pipeline Orchestrator связывает роли в расширенный контур: `ProjectMapReport -> ArchitectureDecisionRecord -> TechnicalSpec -> ImplementationPlan -> TestPlan -> ReviewFindings`. В базовом режиме он сохраняет общий отчет и возвращает `next_action`, но не исполняет Foundry, не меняет registry и не пишет код. Явный режим `--run-transform` может запустить Project Transform только до `promotion_ready`; promote остается отдельным осознанным действием. Это coordination layer для L4 artifacts, а не новый executor. ImplementerSkill становится основным следующим шагом только после стабилизации foundation-перехода `ProjectMapReport -> ADR -> TechnicalSpec`.

Role Pipeline Field Trial прогоняет этот контур по benchmark corpus и измеряет качество рельсов: наличие всех role artifacts, валидный `next_action`, отсутствие source/registry changes, отсутствие Foundry/LLM invocation по умолчанию. Дополнительный `implementation_score` проверяет, что `ImplementationPlan` явно связан с `TechnicalSpec.extraction_contract`: target совпадает с selected candidate, input/output contract перенесен в `contract_binding`, а benchmark target совпадает с `expected_best_extraction_candidate`. `qa_score` проверяет следующий переход: `TestPlan` покрывает тот же `implementation_target` через `test_target`, `contract_test_matrix` и negative tests, а `ReviewFindings` подтверждает coverage и отсутствие contract violations. Это quality gate перед подключением LLM backend к отдельным ролям.

Формулировки L4-артефактов считаются частью контракта качества. `runtime/role_artifact_quality.py` детерминированно оценивает `ProjectMapReport`, ADR и TechnicalSpec по конкретности, source-linked evidence, data lifecycle, subsystem boundaries, interface/error/state contracts, actionable handoff, verifiable acceptance criteria, ranked extraction contract и отсутствию generic placeholder-фраз. Для downstream ролей существующие проверки ImplementationPlan, TestPlan и ReviewFindings сохраняются, но текущий hardening scope ограничен первыми тремя ролями. Gate `tools/role_artifact_quality.py` прогоняется по project-analyzer corpus; успешный результат требует `avg_score >= 0.9` и ноль warnings. Это не teacher ground truth и не LLM-судья, а защитный фильтр против красивого, но непригодного к реализации текста.

Foundation semantic quality дополнительно проверяется через `runtime/foundation_semantic_quality.py` и `tools/role_foundation_field_trial.py`. Этот gate оценивает не только наличие полей, но и полезность перехода `ProjectMapReport -> ArchitectureDecisionRecord -> TechnicalSpec`: конкретность назначения, domain profile, сценарии/входы/выходы, execution path, capability model, contracts/weak zones, error/state/replay model, multistage data lifecycle, actionable extraction plan, перенос domain profile в ADR, tradeoffs/rejected options, source-backed first slice, traceability, negative cases и `TechnicalSpec.human_review`. Пороги, generic-text guards, минимальные counts, веса критических semantic checks и source-reference markers этого gate лежат в `config/foundation_semantic_quality_policy.json`; Python-модуль является интерпретатором policy, а не местом накопления новых role-specific знаний. `artifact_score` остается структурной диагностикой заполненности контрактов; выпускной `overall_score` является минимумом structural и weighted semantic score. Поэтому критический смысловой промах вроде marketing-blurb вместо назначения проекта или generic first slice при явных domain cues не может маскироваться как `1.0/10.0`. Оценка считается по худшему проекту и худшей роли; среднее является диагностикой. Текущий target для расширенного foundation holdout - `>= 9.5/10`; последний measured floor на `github_foundation_holdout_15f`, `15g`, `20b` и `20c` равен `9.6/10`.

Project type coverage является частью KB, а не Python-кода ролей. `knowledge/architecture_patterns/project_archetypes.json` содержит 95 `project_archetype_rule` records, и каждый record обязан иметь `scenario_summary`, `input_summary` и `output_summary`. Эти поля являются API для Project Analyzer: если rule matched, Analyzer переносит их в `ProjectMapReport.answers.1_scope`, а Architect/SpecWriter используют их как evidence/context. Новые классы проектов должны добавляться в KB records и тестироваться через cached/local holdouts; код Analyzer должен оставаться интерпретатором evidence, matcher и artifact contracts. Исключение допускается только для общих extractor возможностей, например source-health, AST, route/import discovery, data lifecycle fallback, module-level source context или scoring policy. Слишком широкие KB-matchers обязаны иметь `required_contains_any`, `required_source_contains_any` или другой token-aware evidence guard, чтобы обычные слова вроде `map`, `graph`, `task`, `archive`, `rich`, `render` или `module` не перехватывали coverage, migration, wheel-packaging, graph-algorithm, documentation, property-testing или hook-runner проекты.

Project recognition является отдельным authoritative gate между Project Analyzer и Architect. `ProjectRecognitionDecision` переносит classification, Analyzer confidence/evidence, ambiguity reasons, role route и pilot route; evidence-match с Analyzer confidence ниже `0.70` не допускается как recognized. Поисковая strata не является classification authority. `ProjectRecognitionCorpusReport`, создаваемый `tools/project_corpus_qualification.py`, отделяет discovery-кандидатов от qualified manifest и позволяет `github_full_chain_probe.py --project-name ...` запускать дорогую цепочку только по прошедшим recognition cases. Неизвестные типы идут в `unknown_project_lifecycle`, неоднозначные - в recognition review, а несовпадение ожидаемой strata сохраняется как rejected evidence, но не стирается из отчета.

Role maturity оценивается не только общим role score, но и матрицей `role x project_stratum x risk_profile`. Политика `config/role_project_type_evaluation.json` группирует точные project archetypes в устойчивые evaluation strata, задаёт evidence thresholds, legacy score ceiling и cross-cutting risk profiles. `runtime/role_project_type_evaluation.py` строит `RoleProjectTypeEvaluationReport` из case-level field-trial evidence, использует worst-case score, отдельно считает blind cases и independent reports и публикует heatmap/priority queue. Старые downstream component scores не могут объявить сектор mature, а `unknown_new_archetype` всегда остаётся classification debt. Новые foundation/downstream field-trial cases сохраняют `project_classification`; backfill старых отчётов может читать только artifact paths внутри workspace. CLI: `python tools/role_project_type_evaluation.py --report <report.json> --blind-report <holdout.json> --format markdown`.

Semantic target families являются configuration-first. `config/semantic_target_profiles.json` содержит 272 профиля, которые задают ranking/quality bonus, benign runtime boundary markers, contract family, input/output contracts, side-effect policy, validation gates и failure modes. В профильном слое уже закреплены не только web/CLI/LLM cases, но и compute-graph blockwise construction, module import graph resolution, crawler settings application, workflow DAG trigger, chat-template rendering, URL query mutation, source-file parsing, framework dependency analysis, SSH connection, template project generation, runtime message dispatch, static-site build, web raw-data rendering, CLI option processing, pipeline registry composition, isolated build environment lifecycle, websocket protocol-send, plugin hook dispatch, package distribution-name parsing, SDK resource factory, dataset loading, app launch, data-expectation metric, SQL translation и tokenizer conversion boundaries. `config/target_quality_policy.json` содержит общую policy для target quality и SpecWriter ranking: support/utility penalties, representative target markers, strong-contract markers, runtime-boundary markers, trivial/bootstrap/liveness/mutation guards и domain/repair/operational ranking groups. `config/technical_spec_policy.json` содержит SpecWriter policy для context-only source filters, snippet unresolved-name guards, inferred argument/payload/result contract types, side-effect process-boundary markers, semantic rerank thresholds, architecture-shape score markers и `first_slice_scope`: при наличии `ArchitectureDecisionRecord.first_slice_contract.targets` SpecWriter обязан выбирать лучший candidate внутри этого bounded set, а не расширять scope из широкого brief. Python-код читает эти records и исполняет общий scoring/contract-inference/alignment contract; добавление нового класса first-slice признаков должно идти через policy/profile + tests/holdout evidence, а не через новую ветку внутри роли. Project Analyzer предоставляет KB matcher ограниченный sampled-source канал (`source_sample_text`) как evidence для доменных правил, но wording-quality gate игнорирует source snippets при поиске generic phrases, чтобы не штрафовать сгенерированные role artifacts за слова из чужого исходника. Если SpecWriter начинает регулярно выбирать валидный, но низкооцененный target, первым маршрутом исправления является новый или расширенный semantic target profile/policy record с tests и field-trial evidence. Обратная сторона также обязательна: если профиль создан только ради одного проекта и не повторился на нескольких confirmed cases, он остается narrow KB/profile record и не превращается в broad rule.

Framework/plugin hardening подтвержден на исходном frozen qualified corpus отчётом `framework_plugin_hardened5_final_20260827_20260827T144802017184Z.json`: `5/5` role chains и executable acceptance, worst-case `0.95`, ноль reselection, failed checks, contract violations, architecture drift и source changes. Независимый owner holdout `artifacts/github_framework_plugin_owner_holdout_20260827` использует explicit `excluded_owners`; purpose-aware matcher отделяет назначение из root docs от случайных dependency/source markers. Qualification `framework_plugin_owner_holdout_ready_20260827_20260827T152026835684Z.json` дала `6/8` recognized cases. Full chain `framework_plugin_owner_holdout_full_chain_20260827_20260827T152226750638Z.json` завершила пять cases как `ok` и один как controlled `blocked_ok`; все пять запущенных Executor acceptance прошли, contract violations, architecture drift и source changes равны нулю. Foundation report `role_foundation_min_field_trial_20260827T152744316021Z.json` локализует остаточный дефект в AutoDoc: Architect `8.33`, SpecWriter `6.47`, usable handoff rate `0.833`. Поэтому promotion всего framework/plugin сектора запрещен; следующий gate должен улучшать только безопасную декомпозицию stateful docs/codegen first slice и связь `Architect -> SpecWriter`, не ослабляя controlled block.

Architect execution-feedback не ослабляет semantic admission: если `ExecutableReselectionRequest` не несёт собственного порога, `executable_acceptance_rejected` использует общий `first_slice_reselection.minimum_semantic_score`. Promoted candidate-selection policy может передать повторно выбранный target в process-isolated acceptance, включая direct effects, но окончательный effect gate независимо допускает только явно разрешённые классы эффектов; например, изолированный `memory_state` допустим, а filesystem/network write остаются заблокированы. Это не позволяет семантически слабому helper обойти роль Architect только потому, что предыдущий target не исполнился.

Architecture synthesis также является configuration-first. `config/architecture_synthesis_policy.json` задает fallback first-slice shape, bottleneck source rules, severity/kind ordering, task-focus priority, verification/defer defaults, confidence thresholds и fallback primary target. `runtime/project_architecture_synthesis.py` должен интерпретировать эти policy records вместе с KB archetype/capability/risk records, а не держать внутри себя новые приоритеты и рецепты.

Crystallization backlog: первая часть старых target/source эвристик уже вынесена из `runtime/target_quality.py` и `runtime/role_spec_writer_ranking.py` в `config/target_quality_policy.json`; часть SpecWriter source-scope, snippet-analysis, type-inference, semantic-rerank и first-slice scope правил вынесена из `runtime/technical_spec_builder.py` в `config/technical_spec_policy.json`; часть Architect fallback/source-selection правил вынесена из `runtime/architecture_decision_builder.py` в `config/architecture_decision_policy.json`; architecture synthesis ordering/defaults вынесены из `runtime/project_architecture_synthesis.py` в `config/architecture_synthesis_policy.json`; semantic quality thresholds/check parameters вынесены из `runtime/foundation_semantic_quality.py` в `config/foundation_semantic_quality_policy.json`; прежние SpecWriter contract-family fallbacks для ML generation, agent consensus и LLM repair hypothesis, а также confirmed holdout families для Dask/DRF/PyInstaller/Scrapy/Airflow/Transformers/Bokeh/DRF/Paramiko/Cookiecutter/Locust/MkDocs/Mypy перенесены в `config/semantic_target_profiles.json`. Эти модули должны оставаться policy/profile interpreter, а не местом накопления новых role-specific знаний. Следующие кандидаты на вынос в config/KB: более богатые KB architecture synthesis recipes и любые новые scoring признаки, которые повторились на нескольких confirmed cases. Перенос допускается только малыми шагами: добавить профиль/rule, покрыть tests, прогнать cached holdouts, затем удалить соответствующую частную ветку из Python.

Тот же gate закреплен для External-3 (`map`, `5`, `004`) и GitHub-10. Эти corpus зависят от локальных/загруженных проектов, не входят в чистый checkout и запускаются в полном acceptance только через `--local-project-trials`; portable CI использует только версиированные benchmark fixtures. Model-backed GitHub L4 quality probe также не является deterministic CI gate и подключается отдельно через `--live-l4`. Для native-heavy или не Python-first проектов допустим явный `blocked_no_safe_candidate` handoff: система должна честно остановить implementation path, сохранить причину блокировки во всех L4-артефактах и не выдумывать source target ради прохождения проверки.

Architect Curriculum Local-3 является отдельным малым gate для teacher-reference улучшения только связки `Project Analyzer + ArchitectSkill`. Corpus живет в `curricula/architect_local_3`; runner `tools/architect_curriculum.py` строит fresh `ProjectMapReport`, затем deterministic `ArchitectureDecisionRecord`, сравнивает их с `teacher_reference.json` и пишет отчет в `artifacts/curricula/`. Teacher reference делится на `facts` и `judgments` и не считается абсолютной истиной: он нужен как проверяемый ориентир качества. Gate измеряет fact recall/precision и judgment score; improvement backlog должен указывать на конкретный недобор анализатора или архитектора, а не на необходимость расширять роли или подключать GitHub.

Важно: curriculum-контур не является самообучающейся системой. Он не генерирует себе ground truth, не принимает свои текущие ответы как эталон и не меняет код автоматически по собственному выводу. Улучшение выполняется как внешний teacher/corrector loop: человек или внешний корректировщик задает или правит reference, запускает прогон, анализирует расхождения, затем инженерно дорабатываются extractor/scorer/ranking/role logic и снова прогоняются тесты. Любые LLM artifacts в этом контуре являются advisory evidence, а не authority.

LLM в Cognitive OS трактуется как ограниченный источник гипотез внутри проверяемой инженерной машины. Модель может предложить интерпретацию, риск, архитектурную опцию, knowledge gap, repair candidate или semantic tie-break, но такой результат не становится истиной и не получает права на исполнение без evidence, typed artifact contract, deterministic hardening, conformance checks и тестов. Рабочий принцип развития: `LLM discovers; Code repeats; Contracts constrain; Tests decide`. Если повторяемый LLM-маршрут можно описать схемами, графом, правилами выбора и машинной проверкой качества, он должен постепенно вытесняться deterministic planner/rule/capability/repair operator, а LLM остается fallback для неизвестных или семантически неоднозначных случаев.

Приоритет вытеснения LLM закрепляется так: сначала L3.5 planning/recovery, затем Tester executable acceptance и Reviewer conformance checks, затем Implementer planning, фактическая часть Project Analyzer, knowledge acquisition planning и memory/template reuse. Architect остается L4-семантической ролью дольше остальных: код готовит evidence, ограничения и варианты, но смысловые компромиссы и новые архитектурные гипотезы допускают LLM или human review. Любой model-backed результат обязан сохранять раздельно raw model output, hardened output, `model_output_clean`, quality warnings, hardening actions и необходимость human review.

Controlled LLM fallback допускается только при отказе deterministic-схемы получить валидный результат. Такой fallback не повышает полномочия модели: LLM возвращает bounded proposal, который снова проходит тот же слой проверок. Для L3.5 это Pipeline DSL validation и MotorPlanPacket contract; для L4 — evidence hardening и raw-vs-hardened audit; для Tester — executable acceptance obligations; для Reviewer — deterministic conformance checks. Если proposal не проходит проверку, система возвращает blocked/needs_human_review, а не исполняет догадку.

Tester executable acceptance v0.3 реализует первый project-specific harness поверх `TestPlan.executable_acceptance`: `runtime/executable_acceptance.py` генерирует pytest scaffold, запускает его в sandbox/work directory и сохраняет `ExecutableAcceptanceResult`. Scaffold всегда проверяет структуру obligations, наличие positive contract case, controlled malformed-input case и side-effect boundary case. Для простых целей вида `file.py:function`, расположенных внутри project root, scaffold импортирует функцию через isolated import, вызывает ее с sample kwargs из positive contract case, проверяет output shape и требует controlled failure на malformed input. Классы, методы, async functions, framework handlers и targets вне project root остаются meta-checked до следующего harness stage. Reviewer читает `ExecutableAcceptanceResult` из `TestResult.executable_acceptance_result` и переводит failed result в conformance failure.

Верхний слой разделяется на L4.0 Cognitive Control Plane и L4.5 Semantic Reasoner. L4.0 является кодовым контроллером повторяемых решений: role transition state machine, artifact promotion gate, semantic escalation policy, prompt/goal gates, quality gates и crystallization backlog. L4.5 остается LLM/human-assisted semantic layer для неоднозначных целей, trade-off решений, неизвестных маршрутов, новых capability designs и доменной оценки. Инвариант: L4.0 может выбирать из известных маршрутов и блокировать нарушение контрактов; L4.5 может предлагать новые гипотезы, но не обходит gates.

Crystallizing Cortex: если решение повторяется, оно мигрирует из L4.5 в L4.0 policy/template/gate/test. Если решение проверяемо, оно мигрирует в deterministic gate. Если решение остается спорным или смысловым, оно остается L4.5 advisory. Текущая реализация L4.0 начинается с `runtime/cognitive_control_plane.py`, который строит `CognitiveControlPlaneDecision` с `artifact_promotion_gate`, `role_transition`, `semantic_escalation` и `crystallization_backlog`. Повторяемые правила дополнительно представлены как `L4DecisionTable` в `runtime/l4_decision_table.py`: это декларативная карта уже кристаллизованных решений, а не новая роль. Для Stage 2 prompt-to-product тот же control plane работает в режиме `prompt_to_product`: `PromptAdequacyGate + supported_template -> prompt_product_gate -> role_transition`. Сборка `VerifiedSystemPackage` разрешена только при `role_transition.next_action=build_verified_system_package`; явно vague или risky `needs_clarification` возвращает уточнение, `unsupported` останавливает маршрут, а bounded prompt без supported template или concrete bounded implementation prompt с intake uncertainty становится кандидатом на L4.5 hypothesis proposal без обхода contracts.

Для Stage 2 пользовательским результатом является рабочий generated project directory, а не промежуточные отчеты ролей. `GoalSpec`, `ProductTechnicalSpec`, `ImplementationPlan`, `SandboxOperationGraph`, tester review и release records являются typed API artifacts между уровнями, ролями и gates. `runtime/generated_product_quality.py` добавляет выпускной `GeneratedProductQuality` gate с порогом `0.92`: пакет должен иметь source/tests/README, CLI/service entrypoint, явный input/output contract, controlled error path, run/test instructions, project-scoped verification, positive release decision, чистый release directory без runtime cache и case-specific behavior. Успешные role gates без прохода product-quality gate не считаются достаточным результатом для above-MVP prompt-to-product.

Configuration-first runtime теперь имеет собственные диагностические API. `ConfigDoctorReport` проверяет загрузку и cross-reference integrity внешних каталогов; `ConfigCoverageReport` показывает, какие templates/rules/transforms/profiles реально покрыты tests/trial registries; `RuleTrace` встраивается в `VerifiedSystemPackage` и объясняет, какие config sources участвовали в решении; `ConfigMutationSandboxReport` валидирует proposed JSON replacement без изменения активного файла. Эти artifacts не добавляют новую роль и не расширяют authority LLM: они защищают инвариант, что L4/L4.5 являются интерпретаторами внешних правил.

L4.5 обязан быть видимым контрактным переходом. Если `semantic_escalation.l4_5_required=true`, runtime формирует `SemanticEvidencePack` и `SemanticHypothesisRequest`: source decision, trigger reasons, evidence context, allowed hypothesis types, forbidden actions, output contract и return path. Evidence pack содержит prompt facts, failed gates, known templates и authority=false, чтобы L4.5 не фантазировал за пределами фактов и не получал исполнительных прав. L4.5 может вернуть только bounded `SemanticHypothesisProposal`; он не имеет права исполнять pipeline, строить package, менять source tree или registry, promote capability или обходить L4.0/L3.5/L2 gates. Возврат всегда идет обратно в L4.0 через `L4SemanticValidationResult`: L4.0 отдельно проверяет contract validity, evidence, risks, confidence, return-to-gate, forbidden actions, prompt boundary policy и отсутствие claims о runtime mutation; результат содержит machine checks, `policy_review` и human-readable explanation. Реальный model-backed L4.5 включается только явно и работает через OpenAI-compatible gateway; raw model output нормализуется, forbidden actions отбрасываются, proposal валидируется, а при ошибке provider выполняется deterministic fallback. Режимы качества модели фиксируются явно: `deterministic`, `model_propose_only`, `model_with_human_review`, `blocked_model_untrusted`. Для Stage 2 ready prompt без supported template и для concrete bounded implementation prompt с `prompt_intake_uncertainty` L4.5 сначала обязан попробовать existing means из evidence pack. Если найден проверяемый маршрут, результатом становится `SuccessfulResolutionCandidate`: он фиксирует means used, verification plan и слабый KB candidate, но не попадает в активную KB до нескольких успешных повторов и review. Если существующих средств недостаточно, результатом становится `DeveloperImprovementRequest` для Codex/human доработки с acceptance focus. Legacy `new_template_candidate` остается совместимым типом proposal, но не является первым маршрутом после отказа gate. Если prompt boundary равен `unsupported_product_surface`, L4.0 маршрутизирует даже developer request в `ask_clarification`; vague prompts, secret/live-risk markers и unsupported surfaces не превращаются в developer work. `SemanticProposalReplay` сохраняет request/evidence/proposal/hardening/validation/outcome для сравнения моделей и промптов. `L45SemanticBenchmarkReport` измеряет deterministic cases верхнего semantic-loop и входит в portable acceptance. Перевод candidate в поддержанный deterministic template проходит через review/admission, teacher-reference quality, isolated scaffold, project-scoped tests, tester review и отсутствие source/registry mutations.

Programmer strengthening следует тому же принципу. Для bounded implementation prompt без поддержанного template Stage 2 может использовать `LLMSandboxImplementationResult`: operation берется из `registry/sandbox_programmer_operations.json`, `text_expression` проходит AST hardening, profile-based operations исполняются только через allowlisted stdlib branches (`csv_*`, `html_table_to_csv`, `json_*`), package создается только в sandbox, compile/pytest обязательны, а `SandboxProgrammerAdmission` отдельно проверяет CLI boundary, README, tests, fixtures, negative test и invariants. Если deterministic registry match не найден и включен `use_model`, L4.5/Deepseek может вернуть только существующий `operation_id` из registry с confidence и reason; invalid id, low confidence и provider error блокируются до сборки package. Каждый `SandboxImplementationPlan` обязан включать `SandboxOperationGraph`: machine-readable цепочку `read_input -> parse_input -> transform -> serialize_output -> write_output -> verify` с parser/serializer selection, data contracts, side effects, evidence и invariants `raw_model_output_executed=false`, `source_tree_changes=false`, `registry_changes=false`. Этот граф является межролевым API для L4/L4.5/programmer/tester/admission, а не человеческим описанием и не разрешением на исполнение произвольного кода. Успех дает максимум `release_ready_with_risks`; активная KB/template запись или изменение пользовательского проекта требуют отдельного human-reviewed шага.

L4.5 live quality trial не является acceptance gate по умолчанию. Он представлен `L45SemanticComparisonReport`: deterministic benchmark report сравнивается с model-backed report по action match, validation pass rate, fallback count, hardening actions и interpretation. Текущий default live-профиль использует `deepseek/deepseek-chat`; модель вызывается только на escalated cases и после hardening не получает authority. Это подтверждает текущую политику: LLM остается ограниченным источником гипотез и материалом для replay review, а не authority. Если повторяемая модельная гипотеза полезна, ее нужно кристаллизовать в L4.0 rule/template/test.

Исходная последовательность role curriculum была safety rail: Architect Local/External, затем Project Analyzer/Architect gaps, SpecWriter и только потом downstream-роли. Эта последовательность завершена и сохраняется как историческое объяснение зависимостей gate, а не как текущий backlog.

Role hardening sequence `Reviewer -> Implementer Planner -> Programmer Executor -> Researcher benchmark -> blind executable transfer` завершена на уровне bounded MVP gates. Профиль `deterministic_filesystem_read_pilot` допущен только для supervised analysis/sandbox execution с `source_apply=false`. Operational-контур разделен на `PilotRunRecord -> digest-bound human review -> PilotTelemetryReport`: автоматический run останавливается в `awaiting_human_review`, а pending review, handoff loss или source mutation немедленно переводят telemetry в `attention_required`. Первый reviewed batch дал `approve=1`, `rework=2`, `stop=2`, first-pass `0.2`, handoff loss `0`, source changes `0`; отчет `artifacts/pilot/pilot_telemetry_20260827T100335500605Z.json` поэтому остается `attention_required` при gate `0.8`. Актуальный порядок развития: `callable acceptance rework -> new operational confirmation -> reviewed semantic boundary evidence -> profile-by-profile expansion -> production operating model`. Все роли удерживаются regression-наборами; новые изменения допускаются только по конкретному blind или pilot evidence gap. Инвариант остается прежним: `teacher_reference != ground truth`; `facts` проверяются по evidence отдельно от `judgments`, а код не правится автоматически по собственному выводу системы.

SpecWriter Curriculum Local-3 допускается только после зеленого Architect Local/External gates. Его задача — проверить не реализацию и не тестирование, а качество `TechnicalSpec`: extraction contract выбран из ADR evidence, candidate стоит первым в ранжировании, acceptance criteria ссылаются на source evidence, non-goals сохранены, а handoff ограничен следующим role artifact. Implementer/Tester/Reviewer остаются вне scope этого gate.

SpecWriter Curriculum External-3 расширяет этот gate на `map`, `5` и `004` через `curricula/spec_writer_external_local_3`. Он проверяет, что SpecWriter не выбирает первый контракт только по формальной "pure" метке, а учитывает evidence: framework request/response helpers, middleware, global/memory state и broad runtime boundaries должны проигрывать source-backed контрактам с реальной архитектурной ценностью. Актуальные teacher-oriented ожидания: `map -> import_indoc.py:parse_file`, `5 -> app/providers/factory.py:build_providers_from_config`, `004 -> p0042/service.py:resolve`, при этом соседние provider/LLM parser boundaries остаются обязательным evidence/context, а не самовольным writable target. Этот gate остается teacher/corrector benchmark: source projects не правятся, reference не является ground truth, а расхождения ведут к доработке evidence extraction или ранжирования.

Implementer Curriculum Local-3 является следующим узким gate после SpecWriter. Он проверяет только `TechnicalSpec -> ImplementationPlan`: implementation target совпадает с `TechnicalSpec.extraction_contract`, input/output contract перенесены в `contract_binding`, `patch_scope` и `writable_scope` ограничены выбранным candidate, `evidence_scope` сохраняет соседний read-only контекст из spec, expected files выводятся только из writable target, rollback покрывает expected files, verification commands присутствуют, acceptance mapping перенесен, registry защищен policy, а handoff идет только к Tester. Этот gate не пишет код, не запускает Foundry, не делает promote и не меняет registry; он проверяет качество work package перед будущим исполнительным шагом.

Implementer Curriculum External-3 переносит тот же gate на `map`, `5` и `004` через `curricula/implementer_external_local_3`. Он проверяет, что implementation plan удерживает target, выбранный SpecWriter External-3, и не превращает соседние risky helpers или report-level risks в основную цель: `map -> import_indoc.py:parse_file`, `5 -> app/providers/factory.py:build_providers_from_config`, `004 -> p0042/service.py:resolve`. Для грязных проектов patch scope может включать соседние evidence sources, но expected files и rollback остаются ограниченными, registry policy запрещает неявные изменения, а live provider/local LLM calls, source edits, Foundry и promote остаются вне scope.

Architect Curriculum External-3 расширяет тот же gate на три живых локальных проекта: `map`, `5` и `004`. Corpus хранится в `curricula/architect_external_local_3` и намеренно остается локальным benchmark, завязанным на абсолютные пути `F:/ubuntu/test/...`; он не должен становиться обязательным portable тестом. Его задача - проверять качество на более шумных проектах: packaged copies, tests/tools/scratch filtering, active-vs-legacy ambiguity, route/API boundaries, LLM/provider orchestration и idempotency risks. Reference может включать `teacher_review`: описание грязного состояния проекта, ожидаемое поведение архитектора, известный шум и `tooling_improvement_targets`. Этот слой фиксирует не обучение модели, а benchmark-driven tool improvement: проекты остаются неизменными, а расхождения ведут к доработке анализатора, ранжирования, фильтров и evidence extraction. Для грязных multi-version проектов, таких как `004`, обязательный expected behavior: не чистить исходник, выделить active source family, пометить legacy analyzer branches как `legacy_noise` и исключить их из first extraction plan/dataflow при сохранении как evidence.

ArchitectSkill может иметь optional LLM advisory backend, но только как bounded overlay поверх deterministic ADR. Advisory backend получает уже построенные options/capabilities/risks, выбирает существующий option id и может добавить короткие risk notes. Он не имеет права генерировать новый исполнительный маршрут, требовать registry mutation или запускать Foundry. При сбое LLM фиксируется deterministic fallback.

### 3. Протокол прерываний (Interrupt Packet)
При падении плагина или пробое лимитов Уровень 3 формирует компактный JSON-пакет прерывания, а не текстовое описание:
```json
{
  "type": "CRITICAL_INTERRUPT",
  "source": "plugin_identity",
  "error_class": "timeout_or_exception",
  "state_ref": "checkpoint_id",
  "input_hash": "hash_string"
}
```

Асинхронное ядро исполняет Pipeline DSL как DAG: узлы без неудовлетворенных зависимостей могут запускаться параллельно, а ссылки `$nodes.<id>.output...` допустимы только на upstream dependency. При interrupt ядро приостанавливает планирование новых задач в текущем конвейере, фиксирует checkpoint состояния и корректно завершает, отменяет или дожидается уже запущенных операций по заданной политике. Snapshot должен быть достаточным для последующего воспроизведения (Replayability): версия pipeline, входные данные, ссылки на артефакты, параметры плагинов, hashes входа/выхода и причина прерывания.

Для каждого node должна быть применима node-level execution policy: timeout, retry budget и controlled cancellation. Timeout классифицируется как `transient`, проходит через общий interrupt/retry путь и не должен превращаться в скрытую бесконечную блокировку worker.

Runtime обязан вести durable execution journal: lifecycle событий pipeline/node (`resumed`, `started`, `completed`, `failed`, `retried`, `switched`) с timestamp, pipeline id, node id, capability id и latency. Journal нужен для replay, аудита и диагностики, а не для свободного текстового рассуждения.

Checkpoint должен поддерживать восстановление выполнения: completed nodes и их outputs не выполняются повторно, downstream nodes продолжают работу по тому же Pipeline DSL после `resume_pipeline(checkpoint_id)`.

Для небезопасных или потенциально зависающих capabilities должен быть доступен process boundary: запуск entrypoint в отдельном child process с kill-on-timeout. In-process режим допустим только для быстрых deterministic capabilities, где риск зависания контролируем.

Runtime должен иметь durable queue для pipeline jobs. Queue сохраняет Pipeline DSL snapshot, root input, priority, attempt count, max attempts, worker id, lease expiration, heartbeat timestamp, status, result/error и timestamps на диск. Claim job выполняется атомарно под lock, terminal statuses не перезаписываются повторным worker completion. Приоритеты jobs учитываются при claim: меньший priority number означает более ранний запуск.

Worker pool исполняет jobs из durable queue до idle или в bounded daemon-like loop, поддерживает ограничение `max_workers` и по умолчанию использует process boundary для параллельного исполнения plugins. Running jobs удерживаются lease/heartbeat; stale или lease-expired jobs должны возвращаться в `queued` через явный requeue policy, пока не исчерпан `max_attempts`, а не теряться после падения процесса. Job-level retry не заменяет node-level retry: job retry перезапускает pipeline job целиком, node retry работает внутри одного execution attempt.

Runtime control plane должен давать оператору минимальные команды наблюдения и управления: queue status, job inspect, job cancel, job requeue и runtime health report. Control plane работает поверх durable artifacts, registry, quality metrics, failures и execution journal; он не исполняет plugins и не меняет Pipeline DSL.

Runtime smoke suite должен запускать стандартные проверки одной командой: tests, compile, plugin check, registry doctor и MVP smoke scenarios.

### 4. Локальный инференс (Уровень 3.5)
Runtime поддерживает подключаемый backend локального инференса через OpenAI-compatible endpoint. `vLLM`, `Ollama`, `llama.cpp` и другие серверы допустимы как реализации за этим gateway, если они отдают совместимый chat/completions JSON API.

Локальная модель класса 7B-14B используется как быстрый транслятор паттернов, а не как автономный агент. Ее разрешенный профиль задач:

1. Генерация графа вызовов плагинов по целевой спецификации и схемам из Capability Registry.
2. Выбор capabilities по совместимости входных и выходных контрактов.
3. Проверка совместимости промежуточных артефактов между шагами pipeline.
4. Первичная моторная адаптация при штатных сбоях: retry policy, alternate parser, fallback source.
5. Компактная нормализация диагностического контекста перед передачей на Уровень 4.
6. Сигнализация по deterministic project-analysis reports: короткие machine impulses о candidate capabilities, broad functions, weak contracts, entrypoints, pipeline candidates, recovery loops, subsystem hotspots, ownership boundaries, architecture hotspots, mixed responsibilities, hidden orchestrators, idempotency risks, quarantine candidates, process-boundary candidates, checkpoint candidates, MVP extraction candidates и местах, где нужно решение человека или Уровня 4.

Выход Уровня 3.5 всегда валидируется через JSON Schema / Pipeline DSL. Невалидный выход не исполняется и превращается в interrupt для Уровня 4. Уровень 3.5 не имеет права напрямую исполнять плагины, изменять Capability Registry, создавать новые capabilities или обходить side-effect manifest.

Local LLM planner path обязан принимать от модели только JSON proposal, преобразовывать его через deterministic planner adapter и запускать `validate_pipeline()` перед любым enqueue/execute. Ошибка локального backend или невалидный JSON является controlled failure, а не поводом исполнять свободный текст.

Основной программный фасад Уровня 3.5 — `runtime/spinal_planner.py`. Он принимает `IntentPacket` от L4, строит валидированный `Pipeline DSL`, выпускает `MotorPlanPacket` для L2 и короткий `SignalPacket` для L4. Порядок выбора: зрелый memory template, deterministic required-capabilities chain, rule-based graph и только затем optional local LLM proposal. Ни один путь не может обойти `plan_from_spec()`/`validate_pipeline()`. При невозможности построить план L3.5 возвращает blocked `SignalPacket` с `needs_l4_decision=true`, а не исполняет частичный или текстовый план.

Для interrupt path `runtime/spinal_planner.py` принимает `InterruptPacket` от L2 и выполняет только моторную адаптацию внутри уже утвержденного намерения: `RETRY`, совместимый `SWITCH_PLUGIN`, controlled `STOP` или сигнал о необходимости `GENERATE_SPEC`. Метрика качества L3.5 задается `runtime/spinal_quality.py`: typed packets, validated pipeline, known capabilities, отсутствие прямого исполнения plugins и bounded escalation.

Обязательный вертикальный координатор пользовательских целей — `runtime/goal_runtime.py`. `tools/goal_run.py` не имеет права напрямую вызывать deterministic, graph или LLM planner и не собирает `MotorPlanPacket` самостоятельно. Координатор валидирует intent/motor/signal contracts, передает Pipeline DSL в L2 и собирает `ExecutionEventPacket`/`InterruptPacket`. Тот же `SpinalRecoveryController` используется sync executor, async executor и durable worker pool. Queue result сохраняет `layer_packets` и `level35_adaptations` как replay/audit trace. Каждый последовательный отказ, включая отказ fallback capability, создает новый `InterruptPacket` и снова проходит через L3.5. Recovery ограничен `max_adaptations`; исчерпание бюджета дает typed blocked `SignalPacket` и `STOP`, а не свободный retry loop.

Project-analysis L3.5 path обязан получать только compact fact digest из уже построенного `project_map_report`, а не читать проект напрямую. Его выход сохраняется в goal report как `level35_project_signals` и является коротким advisory signal packet, а не человеческим объяснением. L3.5 обязан иметь deterministic enrichment fallback для subsystem/hotspot/boundary signals, чтобы сбой локальной модели не обнулял полезные импульсы. Он не может менять Pipeline DSL, registry lifecycle, execution state, memory templates или ответы deterministic report.

### 4.1 Межслойные packet-контракты

Мозг, спинной мозг и runtime общаются не свободным текстом, а typed packets из `runtime/layer_packets.py`. Общий envelope: `schema_version`, `packet_id`, `packet_type`, `source_layer`, `target_layer`, `created_at`, `correlation_id`, `payload`.

Допустимые маршруты:

* `IntentPacket`: L4 -> L3.5. Содержит intent, objective, constraints, expected artifacts и success criteria.
* `MotorPlanPacket`: L3.5 -> L2. Содержит planner, capability chain, execution policy и validation summary.
* `SignalPacket`: L3.5 -> L4. Содержит короткие machine impulses, флаги `needs_l4_decision` и `blocked`.
* `ExecutionEventPacket`: L2 -> L3.5. Содержит node/capability lifecycle event и artifact refs.
* `InterruptPacket`: L2 -> L3.5. Нормализует controlled failure/interrupt без проброса сырого traceback как машинного протокола.

Все packets versioned, correlated через goal/session id, валидируются до сохранения и могут быть положены в goal report как replay/debug artifacts. L4 не имеет права отправлять `MotorPlanPacket` напрямую в L2; L3.5 не имеет права заменять `SignalPacket` человекочитаемой интерпретацией.

### 4.2 Принятые решения по открытым архитектурным развилкам

**Граница L3.5/L4 при сбоях.** L3.5 может выполнять только локальную моторную адаптацию внутри уже утвержденного намерения: повторить шаг по retry policy, выбрать совместимый fallback, остановить execution branch и отправить `SignalPacket`. Любое изменение цели, ослабление ограничений, запуск Foundry, пересборка capability или смена пользовательского результата требует L4 decision. Обоснование: это оставляет быстрые реакции быстрыми, но не дает локальному planner незаметно менять смысл задачи.

**Knowledge Gap Loop.** Недостаток знания является отдельным типом состояния, а не поводом для L4-фантазии. L4 фиксирует `KnowledgeGap`: question, needed_for, acceptable_sources, disallowed_sources, confidence_required, expiry_policy и decision_if_unresolved. Для проектного анализа используется расширенный `KnowledgeGapPacket`: question, needed_for, role, reason, acceptable_sources, confidence_required и decision_if_unresolved. L3.5 строит bounded acquisition plan из доступных information capabilities. L1 исполняет конкретные действия: read local files, inspect installed packages, fetch allowlisted official docs, query memory/dialogue, run safe probe, ask user или optional GitHub/docs search capability. Результат возвращается как `KnowledgeArtifact` или `SourceDigest`: source, evidence/facts, extracted_fact, confidence, collected_at, expires_at, limitations и trace to gap. L3.5 не интерпретирует artifact в человеческом виде; L4 использует его как evidence для route/role decision.

**Role Knowledge Base.** Knowledge Base является API между ролями, а не общей энциклопедией. Records могут иметь `role_scope` (`project_analyzer`, `architect`, `spec_writer`, `implementer`, `tester`, `reviewer`, `researcher`) и `evidence_strength` (`weak`, `medium`, `strong`, `verified`). Legacy records без явного role scope получают deterministic defaults в `runtime/role_knowledge.py`. KB records остаются advisory: они не являются ground truth, не исполняют код и не меняют runtime state.

**Knowledge Usage Telemetry.** Использование KB в реальных прогонах фиксируется явным append-only журналом `artifacts/telemetry/knowledge_usage.jsonl`, а не восстанавливается косвенно из отчетов. Событие `KnowledgeUsageEvent` содержит consuming role, event type, source, status, confidence, `rule_id`/`gap_id`, query и compact details. Минимальные события: `kb_preflight_skipped`, `kb_preflight_gap`, `kb_rule_match`, `external_knowledge_probe`. Отчет строится командой `python tools/knowledge_usage_report.py --root .`. Эта telemetry является evidence для оценки покрытия ролей и качества KB, но не дает права auto-promote записей.

**KB Admission Gate.** Пополнение активной KB не является самообучением. Новый record сначала оформляется как `KnowledgeCandidate`; promotion запрещен, пока не выполнены условия: несколько подтвержденных кейсов, external teacher/corrector approval и Codex/developer approval. `teacher_reference` является проверяемым ориентиром, а не ground truth; facts проверяются evidence, judgments оцениваются отдельно. Реализация gate находится в `runtime/knowledge_admission.py`.

**Session Memory.** На текущем этапе вводится легкая session/dialogue memory как advisory context для L4, но не как исполнительный слой. Она хранит темы, решения, открытые вопросы и связи с прошлыми goal reports. Обоснование: контекст диалога уже влияет на качество L4-решений, но прямой доступ памяти к registry/pipeline создаст скрытую оркестрацию.

**Формат внешнего мышления L4.** Система не сохраняет chain-of-thought. Сохраняются только проверяемые внешние артефакты: `decision_summary`, `chosen_option`, `rejected_options`, `risk_notes`, `open_questions`, `confidence`, ссылки на факты и packets. Обоснование: этого достаточно для replay/debug и аудита без превращения внутреннего рассуждения модели в зависимость runtime.

**Capability Registry как источник истины.** Capability Registry является источником истины для lifecycle/status/quality выбора, но не исполняет контрактную проверку один. Contract Registry является обязательным enforcement-фильтром поверх него. Обоснование: registry хранит состояние инструмента, contract registry проверяет границы использования; разделение уменьшает coupling и делает drift заметным.

**Contract Registry.** Контракты capabilities, packet routes и будущие project-analysis contracts должны иметь единый catalog API. На MVP уровне он производный и read-only. Обоснование: L4/L3.5 должны планировать по формальным входам/выходам, а не по именам файлов или неявным соглашениям.

**MVP benchmark.** Успешность MVP измеряется не красотой одного отчета, а воспроизводимым прогоном на нескольких проектах с одинаковыми вопросами: полнота ответов, корректность evidence, полезность extraction plan, наличие packets/replay artifacts и controlled fallback при сбоях. Обоснование: это проверяет архитектуру как runtime-систему, а не как разовый генератор текста.

Первый закрепленный benchmark - `Project Analyzer Field Trial v0.1`. Corpus хранится в `benchmarks/project_analyzer/projects`, каждый проект имеет `expected_analysis.json`, а `tools/project_analyzer_benchmark.py` считает coverage, misses и false positives по категориям анализа. Следующие улучшения Project Analyzer должны подтягиваться через провалы этого benchmark, а не через добавление новых слоев.

### 4.3 Автономная проверка гипотез развития

Foundation self-improvement реализует цикл `measurement -> FailurePacket -> L3.5/L4.5 diagnosis -> bounded trials -> post-training admission -> hypothesis holdout -> corpus verification/rollback`. После измеренного улучшения `runtime/self_improvement_hypothesis_validation.py` строит переносимый `HypothesisValidationPlan` из failure class и структурного evidence без путей исходного проекта. Сетевой discoverer является инъецируемой capability; текущий адаптер `tools/self_improvement_project_discovery.py` использует ordered chain `GitLab -> GitHub`, замораживает bounded reserve unseen проектов, записывает provider attempts/provenance и запрещает повтор одного repository name между forge. Metadata search формирует широкий recall-oriented pool; `runtime/hypothesis_structural_screen.py` ограниченно сканирует product Python AST, использует общий `infer_source_contract`, ранжирует effect/output-basis evidence и замораживает shortlist в `structural_screen.json`. Этот report является только retrieval evidence и не заменяет role probe. Probe gate требует настроенный общий минимум и минимум новых matches; свежий holdout обучается первым, prior evidence становится regression-контролем. Clone stage использует bounded worker pool, а ошибка одного checkout не обрывает frozen selection. Подготовленный baseline/diagnosis переиспользуется.

Начиная с v18 metadata search сначала использует составные запросы `effect + output basis`. `FailureDiagnosisEnvelope` хранит failure class, обязательные effects, output family и допустимые сырые формы evidence. Probe классифицирует совпадение как `exact`, `compatible`, `near_contrast` или `unrelated`: контраст сохраняется как отрицательное evidence, но не допускается к тренировке. `recovery_metrics` измеряет matching yield и signature discrimination отдельно от role readiness.

Holdout-проекты последовательно проходят тот же `train_on_project`, поэтому накопление evidence, post-training admission и promotion выполняет Cognitive OS. Активироваться могут только allowlisted config/policy targets зарегистрированных improvement plugins. Обычный `KnowledgeCandidate` не имеет права на automatic merge. После любого promotion исходный corpus измеряется неизменным evaluator; изменение promotion transaction paths откатывается при регрессии или отсутствии подтвержденного corpus effect.

`HypothesisValidationTrial` различает `completed`, `not_applicable` и `blocked`. Ошибка внешнего поиска, timeout или rate limit записывается как `external_discovery_failed`, нехватка совпавших проектов как `insufficient_matching_holdouts`, а отсутствие свежего независимого совпадения как `insufficient_new_matching_holdouts`; эти состояния не разрешают ослабить minimum evidence. Полный trial сохраняется в `artifacts/self_improvement/hypothesis_validation_*.json`. Малый similarity holdout проверяет одну гипотезу; большой blind multi-type corpus остаётся независимым release/calibration gate.

При нехватке двух совпадений Cognitive OS выполняет до `maximum_discovery_rounds` bounded-раундов. Каждый следующий раунд получает отдельный frozen selection `<hypothesis_id>_rN` и исключает все уже измеренные проекты. Progress sink публикует начало/завершение discovery round, каждого probe и holdout training; ошибка отдельного probe сохраняется как `probe_failed` и не скрывается зависанием или общим crash.
Provider pagination также является частью round contract: `search_page_start` сдвигается на `maximum_search_pages`, поэтому последовательные раунды не повторяют одно star-sorted окно.
Adaptive holdout использует отдельные bounded `provider_policy_overrides`; например, GitHub star floor может быть ниже release-corpus floor. Override не изменяет corpus policy, evaluator и promotion gates.
Search cursor хранится логически по каждому query внутри нормализованной failure signature: старый query продолжает со следующего durable page, новый начинает с page 1. Раунды используют bounded query batches, а provider boundary переводит GitHub qualifiers в обычный GitLab search text. Это расширяет только candidate supply; structural screen, full role probe и admission gates не ослабляются.
Exact evidence переживает evolution search policy: новый trial читает предыдущие durable `HypothesisValidationTrial`, принимает только project paths внутри `artifacts/hypothesis_holdouts`, повторно запускает probe текущим evaluator и засчитывает всё ещё совпадающую подпись. Такой проект исключается из network discovery, но не теряется из accumulated evidence.

Portable signature отделяет семантический контракт от синтаксического evidence через `hypothesis_holdout.signature_normalization`. V11 объединяет `no_value_return` и `explicit_none_annotation` в `void_side_effect`, сохраняя raw basis в probe report. Если минимум три проекта этой семьи не дают применимого candidate-selection challenger и хотя бы один независимый holdout подтверждает `no_structural_discriminator`, trial обязан завершиться `capability_development_required` с запросом `candidate_selection_discriminator_plugin`; продолжение keyword tuning или ручной ремонт проектов запрещены request contract.

Plugin Foundry сопоставляет typed capability request с `implements_capabilities` зарегистрированных improvement plugins. `candidate_selection_discriminator` выполняет bounded shadow trials только по уже найденным ranked/reselection/ADR targets. Подтверждённый shadow является применённым training treatment независимо от durable promotion. Admission получает frozen measured effect, синтезирует policy из парных failed/successful structural contracts и применяет её через штатный SpecWriter preflight и Architect reselection. Текущий проект исключается из training family и может служить только независимым holdout; полный staged contrast используется, если compact runtime report потерял structural fields. Legacy policy без `activation_state` остаётся неактивной, но может пройти повторную аттестацию: недостижимый preflight ремонтируется только в trial-кандидате, policy-backed SpecWriter target переносится в Architect-owned first slice, ordinary route обязан воспроизвести score/status/executable signal, после чего запускаются regression cases. Promotion работает транзакционно и допускает config-bounded последовательность evidence-derived refinements. Общие trigger-дискриминаторы хранятся в `common_requirements` и AND-ятся с каждой OR-альтернативой; controls повторяются заданное число раз. Holdout reproduction, regression gate и внешний corpus gate используют один полный execution-feedback evaluator. Ошибка reproduction, неустранённая регрессия или исчерпание бюджета приводит к rollback и rejection. Допуск policy-backed target к executable acceptance задаётся `foundation_evidence.promoted_selection_policy_admission` и не ослабляет проверки effects, complete source body, state mutation и process isolation. Engine fingerprint инвалидирует старый checkpoint после изменения runtime/config, а provenance learned policy переносится через Architect reselection и rebuilt `TechnicalSpec`.

Подтверждённый shadow challenger передаётся admission-плагину оркестратором цикла как frozen control/treatment evidence; прямые вызовы plugin-to-plugin запрещены. Повторные `isolated_process_failed` ограничиваются config-owned resource circuit breaker: после исчерпания лимита сохраняются лучшие role artifacts и точная причина отказа, а дальнейшие target retries прекращаются до внешнего watchdog.

Search query planning является configuration-first. `hypothesis_holdout.signature_query_terms` сопоставляет effect и output-basis dimensions переносимой подписи с отдельными поисковыми терминами; planner чередует их и сохраняет один bounded failure-class fallback. Пустая выборка работающих providers является empty round, а не `external_discovery_failed`. Расширение vocabulary не требует изменения role/runtime source.

Если исходный проект и diagnosis-matched holdouts повторно исчерпывают bounded search, trial возвращает `decision=capability_development_required` и typed `CapabilityDevelopmentRequest`. Внешний инженер получает право реализовать или починить только указанный improvement-plugin contract, но не исправлять тренировочные проекты вручную.

### 5. Кузница возможностей (Уровень 3.2)
Уровень 3.2 отвечает за controlled build lifecycle capabilities. Он не является обычным plugin и не исполняет пользовательские pipeline. Его задача — создавать и пересобирать каталоговые plugins через проверяемый процесс.

Разрешенные операции Уровня 3.2:

1. `GENERATE_SPEC`: создать типизированную спецификацию capability.
2. `SANDBOX_BUILD`: создать candidate directory в изолированной зоне.
3. `TEST`: запустить контрактные тесты candidate.
4. `REGISTER`: обновить Capability Registry после успешной проверки.
5. `PROMOTE`: перенести candidate в `plugins/<plugin_id>/` и сделать capability доступной runtime.
6. `REBUILD_CAPABILITY`: пересобрать quarantined capability с сохранением контрактов.

Уровень 3.2 не имеет права:

* напрямую менять активный plugin без promotion gate;
* обходить sandbox/test/register/promote;
* выбирать стратегическую цель вместо Уровня 4;
* исполнять runtime pipeline вместо Уровня 2;
* создавать скрытые зависимости между plugins.

Admission gate обязан требовать не только happy-path contract tests, но и negative tests для отказа на плохом входе или нарушении контракта. Candidate без negative tests не может пройти promotion.

Каждый candidate обязан иметь `spec.json` и `requirements.lock`. `spec.json` фиксирует reusable purpose, input/output contracts, error policy и side effects. `requirements.lock` фиксирует внешние зависимости; любые внешние зависимости должны быть pinned через `==`. Promotion report обязан включать spec и dependency probe.

Static admission блокирует опасные imports/calls (`subprocess`, raw sockets, dynamic code execution) и filesystem operations без соответствующего side-effect permission. Side-effect manifest является исполняемым gate, а не декоративным описанием.

Foundry обязан поддерживать generate/validate spec tools и dry-run promotion. Dry-run выполняет все admission/probe/test/quality gates без копирования candidate в `plugins/` и без изменения registry.

### 5.1 Жидкий граф (Уровень 4)
Уровень 4 является стратегическим orchestrator, а не executor. Перед входом в Уровень 4 пользовательский prompt проходит Goal Intake: `runtime/goal_intake.py` строит `GoalSpec` со `schema_version`, intent, target, inputs/outputs, constraints, success criteria, allowed actions, assumptions, `ambiguity_score`, `field_confidence` и optional `ClarificationPacket`. `GoalSpec` валидируется JSON Schema contract с `additionalProperties=false`; невалидный или неполный prompt возвращает `ASK_CLARIFICATION` до вызова L4/L3.5. Clarification loop продолжает существующую `goal_id`, сохраняет исходный prompt, answers и `effective_goal`, после чего пересобирает `GoalSpec`.

Уровень 4 принимает валидированный `GoalSpec` как factual input и возвращает строго типизированное route decision: `PLAN_WITH_L35`, `ASK_CLARIFICATION`, `REQUEST_CAPABILITY_SPEC` или `STOP_UNSUPPORTED`.

Если route или role decision требует факта, которого нет в `GoalSpec`, memory, registry или source evidence, Уровень 4 обязан вернуть `NEEDS_INFORMATION`/`KnowledgeGap` вместо домысливания. На MVP этапе этот путь может понижаться до `ASK_CLARIFICATION`, если допустимый источник только человек, или до controlled stop, если нет разрешенной acquisition capability.

Уровень 4 не имеет права:

* напрямую исполнять plugins;
* менять Capability Registry;
* создавать или продвигать candidates;
* обходить validation Pipeline DSL;
* отдавать свободный текст как машинный протокол.

Единственный путь к исполнению: `Level 4 decision -> Level 3.5 planner -> Pipeline DSL validation -> Level 2 runtime`.

Level 4 обязан сохранять goal session trace: исходная цель, `GoalSpec`, clarification answers, `effective_goal`, route decisions, deliberation artifact, L3.5 plan, execution result и final report. Это операционная эпизодическая память, а не долговременное знание.

Level 4 deliberation является auditable reasoning snapshot, а не исполняемым протоколом. Он фиксирует route, capability summary, memory/dialogue context, risk list, route alternatives, selected alternative и recommendation. Deliberation не имеет права заменять typed route decision, Pipeline DSL validation или policy gates.

Для project-analysis целей Уровень 4 отвечает за человеческую интерпретацию результата. Он получает expanded bounded deterministic facts и `level35_project_signals`, после чего сохраняет `level4_project_interpretation`: executive summary, capability decomposition, refactor plan, cognitive loop, open questions и confidence. После интерпретации допускается deterministic synthesis в `analysis_tasks`: proposed backlog с task id, типом, target, priority, evidence и acceptance criteria. Поверх backlog сохраняется `architecture_synthesis`: проектный диагноз, целевая архитектурная форма, top bottlenecks, matched capability/risk patterns, relevant lessons, recommended first slice, what-not-to-touch-yet и verification plan. Этот артефакт строится knowledge-backed rule engine: runtime загружает декларативные записи из `knowledge/architecture_patterns/project_archetypes.json`, `capability_patterns.json`, `risk_patterns.json` и `project_lessons.json`, подбирает rule/patterns по evidence, фиксирует `matched_rule`/`matched_because` и интерпретирует recipe через allowlisted код. Knowledge base хранит правила/паттерны/рецепты/уроки и `source_scope_policy`, который разрешает использовать docs/examples/tests/extras как evidence, но не как first-slice targets при наличии core-source альтернатив. `knowledge/architecture_patterns/backlog.json` хранит кандидатов на будущие паттерны; это не ground truth и не исполняемый код. Capability/risk/lesson records являются advisory API для L4/Architect/SpecWriter: они подсказывают contracts, mitigations, tests и lessons, но не исполняются и не могут менять registry/runtime state. `analysis_tasks` и `architecture_synthesis` помогают превратить анализ в дальнейшую работу, но не являются Pipeline DSL, не исполняются автоматически и не меняют registry/runtime state. Уровень 4 может быть подключен только к отдельной cortex model с большим контекстным окном; default-профиль использует `deepseek/deepseek-chat` через OpenAI-compatible gateway `http://127.0.0.1:8000/v1` и переопределяется переменными `COGNITIVE_OS_L4_MODEL` / `COGNITIVE_OS_L4_BASE_URL`. Этот профиль отделен от локального Уровня 3.5, модели которого запрещены для L4. Вызов L4 остается явным; при недоступности cortex provider Уровень 4 обязан использовать controlled deterministic fallback без обращения к локальной LLM. Это речь/смысловой слой и постановщик задач, а не машинный протокол исполнения.

Для project-analysis интерпретаций L4 дополнительно проходит deterministic quality gate. `runtime/project_l4_quality.py` оценивает `summary_grounding`, `capability_grounding`, `actionability` и `uncertainty_honesty`; `tools/github_l4_interpretation_probe.py` применяет этот gate к GitHub benchmark corpus. Успешный gate требует внешнего L4-вызова без fallback, отсутствия context-only capabilities и нулевого списка quality warnings. Это защищает систему от красивых, но незаземленных формулировок.

Route alternatives должны оцениваться прозрачно: risk score, cost score, confidence score, blockers, evidence и total score. Минимальный набор путей: mature memory template, deterministic known capability chain, L3.5 LLM planner fallback и terminal alternatives для clarification/spec/stop. Выбор альтернативы не создает Pipeline DSL и не исполняет plugins; он только объясняет, какой planning path должен быть попробован первым.

Поверх goal reports допускается производный Memory Index. Он пересобирается из сохраненных reports, хранит компактные признаки прошлых goals, route decisions, pipeline ids, использованных capabilities и execution status. Индекс является кэшем/навигацией по опыту, а не самостоятельным authority.

Memory recommendation может подсказать `CONSIDER_REUSE_PREVIOUS_PLAN` для похожей успешной цели, но не имеет права напрямую запускать pipeline, менять registry или обходить `Level 4 decision -> Level 3.5 planner -> Pipeline DSL validation -> Level 2 runtime`.

Memory Index может строить plan templates из успешных reports. Template группируется по цепочке capabilities, форме inputs и edges, хранит support count, success/failure counts, safety status и примеры goals. Это процедурная память, но не исполняемый план.

Template safety policy обязана отличать immature, mature и blocked templates. Template становится mature только после нескольких successful reports той же формы и при отсутствии failed reports той же формы. Любой failed report той же формы блокирует template recommendation до будущей явной repair/decay-политики.

Template instantiation обязана учитывать совместимость с текущим контрактом capability chain. Если mature template был построен до изменения входного контракта critical node, он не должен молча обходить deterministic planner. Для project-analysis chain устаревший template, где `read_many_files` использует фиксированный список путей вместо `auto_discover`, считается stale и отклоняется с fallback к deterministic required-capabilities planner.

Template Instantiator может превратить template в candidate proposal только при достаточном score/support, `safety_status=mature` и строго `active` capabilities. `degraded` capabilities допускаются для обычного planner/recovery, но не для быстрого template path. При любой ошибке template path обязан уступить L3.5 LLM planner или остановиться контролируемо.

Для известных Level 4 routes допускается deterministic required-capabilities planner. Он использует строго заданные capability chains, включая `translate_text` и `parse_pdf`, формирует candidate proposal и прогоняет его через `plan_from_spec()` и Pipeline DSL validation. Этот путь должен использоваться после mature template path и до LLM fallback.

Перед route decision допускается memory preflight: система ищет похожие goal reports/templates и сохраняет результат в session/report. Если цель передается в Уровень 3.5, recommendation может быть использована как deterministic template candidate или planning hint, но любой путь обязан пройти deterministic Pipeline DSL validation.

Knowledge preflight является отдельным будущим шагом после memory/dialogue preflight. Он не заменяет runtime memory: memory отвечает "как мы делали похожее", Knowledge Gap Loop отвечает "какого факта нам не хватает и где его разрешено добыть". Любой найденный факт должен быть привязан к source/evidence и не может автоматически менять registry, benchmark или документацию.

MVP-реализация начинается с `runtime/knowledge.py` и L1 capability `inspect_installed_packages`. Первый закрепленный сценарий: legacy `.xls` conversion требует знания о наличии `xlrd/xlwt`; если backend отсутствует, goal report получает `KnowledgeGap` и `KnowledgeArtifact`, а L4 route останавливается с `L4_KNOWLEDGE_GAP_UNRESOLVED` до запуска spreadsheet pipeline.

Official docs и GitHub-поиск оформляются как external evidence sources, а не как источники автоматических решений. L1 capability `official_docs_fetch` читает только allowlisted documentation URLs и возвращает title, excerpt, domain и content hash; `runtime.knowledge.official_docs_knowledge()` оборачивает это в `KnowledgeArtifact` с confidence cap и ограничениями. L1 capability `github_repository_search` выполняет allowlisted GitHub Repository Search и возвращает только metadata: repository name, URL, description, language, stars, owner и license key. `runtime/knowledge_source_policy.py` ограничивает использование external evidence: official docs применяются для проверки API/library facts, GitHub — для pattern/library/edge-case/test inspiration; запрещены verbatim copy, popularity-as-correctness, override official docs и architecture change без локальных tests.

Clarification loop должен продолжать существующую goal session через `goal_id`: `goal_run.py --goal-id <goal_id> --clarification ...` добавляет ответ в session, строит `effective_goal`, пересобирает `GoalSpec` и переиспользует сохраненный `root_input`. Если Level 4 выбирает `REQUEST_CAPABILITY_SPEC`, система может создать typed spec request для Foundry, но не имеет права автоматически генерировать plugin code без Foundry gates.

LLM на Уровне 4 может выбирать только route enum. Невалидный LLM route decision не исполняется и должен иметь deterministic fallback.

LLM route decision проходит post-validation по action-specific invariants: `PLAN_WITH_L35` не может нести `missing_capability_hint` или `clarification_question`, `ASK_CLARIFICATION` обязан иметь вопрос, `REQUEST_CAPABILITY_SPEC` обязан иметь missing capability hint, а `STOP_UNSUPPORTED` не должен переносить executable capabilities. Неконсистентный LLM route понижается до deterministic route decision.

### 5.2 Диалоговая память
Диалоговая память является отдельным слоем и не должна смешиваться с runtime memory. Runtime memory отвечает на вопрос "как выполнить похожую задачу"; dialog memory отвечает на вопрос "о чем мы говорили, что решили, почему и к какой теме нужно вернуться".

Минимальные сущности будущего слоя:

* `dialogue session`: последовательность user/assistant turns и активный рабочий контекст.
* `topic thread`: именованная тема, к которой можно вернуться после переключения контекста.
* `decision record`: принятое архитектурное или продуктовое решение с причиной и датой.
* `open thread`: незакрытый вопрос, гипотеза или отложенная задача.
* `preference/principle memory`: устойчивые правила работы, например "код подтягивается к документации", "templates не обходят validation", "LLM не исполняет, а предлагает".

Dialog Memory не имеет права напрямую менять Capability Registry, строить Pipeline DSL или запускать plugins. Она может давать Level 4 contextual hints: текущая тема, связанные прошлые решения, открытые вопросы и конфликт с ранее принятыми принципами. Любой исполнительный путь остается через `Level 4 decision -> Level 3.5 planner -> Pipeline DSL validation -> Level 2 runtime`.

Статус слоя: thin MVP реализует durable session/topic/decision/open-thread/principle records, recall/summary CLI, deterministic compaction, производный topic graph с merge suggestions и опциональный `dialogue_preflight` в `goal_run.py`. Raw turns остаются источником истины; compact summaries и topic graph являются пересобираемыми contextual hints. Автоматический merge/decay остается следующим этапом; первый успешный Foundry-promote цикл для новой capability закрыт на `translate_text`.

### 6. Сценарий MVP-петли для проверки концепта
Для доказательства жизнеспособности runtime-среды собирается контур из 5 шагов:

**Pipeline:** Запуск цепочки из 3-х простых плагинов: скачать HTML -> распарсить `title` -> сохранить JSON.

**Сбой:** Искусственная подача некорректного URL, битого HTML или имитация блокировки (403 Error).

**Interrupt:** Пороговый фильтр ловит ошибку, приостанавливает pipeline и передает decision layer только JSON-пакет прерывания плюс ограниченный диагностический контекст.

**Decision:** По умолчанию deterministic policy выбирает строго одно действие из системного enum. LLM может подключаться только как optional advisory/route backend поверх typed packets и обязан иметь deterministic fallback.

**Адаптация:** Ядро исполняет выбранное действие через детерминированный контроллер, а не через свободный текстовый ответ LLM.

### 7. Этапы MVP
**Этап 1:** Поддержать только решения `[RETRY, STOP]`. Цель этапа — доказать, что interrupt packet, checkpoint и replay работают без расширения набора инструментов.

**Этап 2:** Добавить `SWITCH_PLUGIN`. Цель этапа — проверить Capability Registry, выбор альтернативного инструмента и совместимость входных/выходных схем.

**Этап 3:** Добавить `GENERATE_SPEC`, но не прямую генерацию исполняемого кода в общий registry. Верхний слой может создать только спецификацию нового capability. Микро-плагин создается в песочнице, проходит тесты, получает паспорт, проверяется по side-effect manifest и только после этого может быть повышен до постоянного инструмента.

### 8. Политика безопасного расширения
Новый capability считается частью системы только после прохождения минимального жизненного цикла:

1. `GENERATE_SPEC`: LLM создает типизированную спецификацию входа, выхода, ошибок и side effects.
2. `SANDBOX_BUILD`: код или адаптер создается в изолированной директории без доступа к секретам и произвольной записи.
3. `TEST`: запускаются контрактные тесты, negative tests и проверка replay на фиксированных входах.
4. `REGISTER`: Capability Registry получает паспорт инструмента, determinism grade, side-effect manifest и hash версии.
5. `PROMOTE`: инструмент становится доступен runtime только после успешной проверки.

### 9. Hypothesis Compiler

Hypothesis Compiler находится между диагностикой и Capability Foundry. Он группирует накопленные project trials по
переносимой сигнатуре отказа, отделяет положительные случаи от контрпримеров и выпускает только типизированный
`HypothesisCandidate`: инвариант, область применимости, требуемый plugin contract и проверяемый validation plan.
LLM может уточнить абстракцию, но не может включить в кандидат код, diff, evaluator/score changes или authority на
promotion. Кандидат становится способностью только через существующий цикл `sandbox -> test -> register -> promote`.

### 10. On-Demand Capability Quarantine
Система не запускает постоянный фоновый аудит всех capabilities. Карантин включается только по факту реального сбоя во время исполнения pipeline.

При падении плагина ядро классифицирует ошибку:

* `transient`: timeout, rate limit, временная сетевая ошибка, временный 403.
* `input_error`: плохой вход, несовместимый формат, отсутствующие обязательные поля.
* `contract_error`: плагин нарушил заявленную output schema или вернул несовместимый артефакт.
* `dependency_error`: `ImportError`, `ModuleNotFoundError`, `AttributeError`, несовместимая версия библиотеки или внешний API.
* `runtime_error`: непредвиденное падение без устойчивого fingerprint.

Плагин переводится в `quarantined`, если:

1. Ошибка классифицирована как `dependency_error`.
2. `contract_error` повторяется на валидных входах больше заданного порога.
3. Hash версии capability изменился, а контрактные тесты не проходят.

При переводе в карантин runtime:

1. Приостанавливает текущий pipeline и фиксирует checkpoint.
2. Обновляет `Lifecycle Status` capability в Capability Registry.
3. Записывает `failure_fingerprint`: error class, traceback hash, input hash, version hash и checkpoint id.
4. Исключает capability из новых pipeline до выхода из карантина.
5. Формирует interrupt для Уровня 3.5 или Уровня 4 с рекомендацией `GENERATE_SPEC` / `REBUILD_CAPABILITY`.

Capability может вернуться в `active` только после прохождения цикла sandbox/test/register/promote. Временные ошибки класса `transient` не переводят capability в карантин и обрабатываются retry policy.

## Project-native repair maturity update, 2026-08-30

Узкий `library_pure_transform` catalog активирован только после явной атомарной транзакции `project_native_repair_promotion_20260830T000805901553Z.json`: три независимых verified sandbox outcomes, complete role-cell matrix, `126` regression tests, Config Doctor `43/43`, line-limit gate и zero source-project changes. Любая регрессия до reload должна откатывать catalog; automatic promotion и source apply остаются запрещены.

CLI/local-tool evidence ведётся отдельно. Первый подтверждённый outcome использует полный Autoflake parent tree `e2109ba97f0590c6b4410e195c426b62235b4a09` и только regression test из read-only fix `aefc058d82c5f9973fc5022e0f8da0a0d7cb4acc`. Failure taxonomy `incomplete_import_token_contract` допускает reducer `guard_incomplete_import_tokens` только для exact AST shape `line.split()[1]` внутри traceback-bound target. Sandbox verification обязана пройти исходный nodeid и полный suite; подтверждённый результат равен `1 passed` и `172 passed`, source digest unchanged. `knowledge/role_knowledge/project_native_cli_failure_repair_patterns.json` остаётся staged при `1/3`; pure-transform maturity не переносится на CLI или широкие strata.

`ProjectDevelopmentExperimentAdmission` обязан исполнять recognition pilot route. Статус `analysis_only_stop` блокирует sandbox experiment независимо от наличия failure-backed reducer; отсутствие этой проверки считается gate bypass. Validated memory из run, чей pilot route не был `eligible_for_full_chain`, не допускается в memory context. Rich-CLI является superseded negative case: первоначальный sandbox outcome не является promotion evidence, исправленный run имеет controlled stop и rejected validated-memory claim. Click strict-default case также остановлен из-за project-wide `stateful/subprocess` risks. Текущий CLI ledger остаётся `1/3`.

Target-scoped waiver проверяется отдельным frozen source-backed contrast gate. Отчёт `project_target_scope_contrast_20260830T145348614957Z.json` подтвердил `3/3` границы: повторяемый pure target isort допускается, target с прямым `open` отклоняется, а независимый Click target не обходит неотменяемый `subprocess` risk. Проверка не пишет source и имеет `capability_effect=no_expansion`. Наличие только одного положительного lineage при минимуме два сохраняет `independent_positive_transfer=pending`; отрицательные contrasts не считаются положительным transfer evidence.

Foundation semantic scoring различает неуспешный handoff и доказанное отсутствие безопасного handoff. `blocked_no_safe_candidate` получает полноценную оценку только при пустом candidate, terminal `resolution_status=exhausted`, authority `architect`, положительном expanded candidate count, сохранённом candidate viability, нуле qualified candidates и пустом selected target set. Для такого исхода executable confirmation не требуется, поскольку исполнение запрещено самим gate; неполное или самодекларированное exhaustion сохраняет unverified/terminal caps. Owner holdout `role_foundation_min_field_trial_20260830T150833324291Z.json` подтвердил правило на восьми проектах: семь executable callable, один controlled stop, zero source changes.

## Framework/plugin and workspace maturity update, 2026-08-30

`framework_plugin_build` имеет три независимых project-native repair lineage: Pluggy hook ordering, PyPA Build wheel `.dist-info` discovery и MkDocs empty theme configuration. Каждый failure повторён дважды и связан с единственным production target; каждый reducer работал только в patch sandbox, прошёл generated-function stub gate, targeted replay, configured regression, reassessment и source invariant. Каталог `knowledge/role_knowledge/framework_plugin_failure_repair_patterns.json` активирован fail-closed транзакцией `artifacts/role_promotion/project_native_repair_promotion_20260830T160749995978Z.json` после `174` regressions и Config Doctor `44/44`.

Token-aware matcher не смешивает присваивания вложенных функций с lexical scope теста. Project probe profile может отключать editable install и source bootstrap для self-hosting library вроде Pluggy. Target-scoped effect matcher допускает `open` только с явным литеральным read-only mode `r`, `rt` или `rb`; отсутствие mode, dynamic mode и write modes остаются запрещёнными. Frozen contrast `project_target_scope_contrast_20260830T161826182156Z.json` совпал `4/4` и достиг `2/2` независимых positive lineages, сохранив direct-write и subprocess negative controls.

Workspace scope-selection stop считается полноценным Analyzer outcome только при двух или более candidate roots, отсутствующем preferred candidate, полном наборе scope checks и нулевых source/registry changes. Downstream execution в этом состоянии не требуется и запрещено. Матрица `artifacts/field_trials/role_project_type_evaluation_20260830T161442270918Z.json` даёт `workspace_portfolio / Project Analyzer = 9.7` по пяти blind projects и пяти независимым reports; остальные роли для этого stratum остаются `not_applicable`.

### 11. Bounded Self-Development Change Protocol

Every system-directed change is represented by `SelfDevelopmentChangeProposal` with problem signature, hypothesis, typed evidence, impact map, verification state, rollback plan and content-bound identity. `config/self_development_change_policy.json` defines the `L0-L4` authority matrix. The interpreter is fail-closed: unknown kinds become L4, runtime changes require external promotion review, and `L1-sensitive` evaluator/admission/promotion changes cannot use the changed success mechanism as their sole evaluator. Initial integration is shadow-only and never authorizes execution, source apply or promotion.

Self-development verification gates require durable content-addressed receipts. `PromotedEvidenceLedgerEntry` binds an immutable evidence copy to SHA-256, distinct producer/evaluator fingerprints, and a replay argv; resolver validation rechecks ledger identity, filename, workspace scope, artifact bytes, JSON result status, frozen holdout provenance, and zero generated stubs. Caller booleans such as `regression_passed` are compatibility data only and provide no admission authority.

Role interaction evidence is localized per handoff rather than inferred only from a final score. `RoleChainInteractionTrace` contains digest-bound producer/consumer contracts, loss reasons, arbitration metrics, unresolved uncertainty, target continuity, reselection history, and human-decision requirements. Role/project-type `mature` status remains a working threshold; `promotion_eligible` separately requires the active narrow lane target `9.7+` and complete evidence. Deferred broad strata cannot become promotion eligible through aggregate role scores. Unknown archetype proposals require confirmed independent lineages, unique evidence digests, and a coherent marker set before teacher review; otherwise they remain quarantined in `collect_more_cases`.

`SelfDevelopmentShadowTrialReport` evaluates classification, typed evidence, project-type and role impact, architectural delta completeness, proposal integrity and authority separation. The initial historical backfill report `self_development_shadow_trial_20260830T175415849133Z.json` passed three mature project types at `3/3` and minimum score `1.0`. Historical promotion is evidence only: prospective detection and a new unseen L0 candidate are still required before any autonomous promotion experiment.

`SelfDevelopmentProspectiveDetectionReport` enforces a timezone-aware evidence cutoff, exact source digests, an allowlist of system-level rule families and at least three distinct projects per portable signature and project type. Subthreshold signatures remain explicit in a digest-bound watchlist with their independence deficit instead of disappearing or becoming candidates. A detected lesson remains shadow-only: it has no patch digest, regression or unseen holdout, so sandbox and promotion admissions stay blocked. Report `self_development_prospective_detection_20260831T031558750274Z.json` has status `waiting_for_evidence`: it covers 114 reports, excludes 75 pre-cutoff reports, observes 34 eligible reports and binds the same recognition-gap signature to two independent projects. One further independent project is required; candidate count remains zero.

`SelfDevelopmentCorpusEligibilityIndex` is the provenance gate for reusing the local corpus. It separates `prospective_used`, `historically_exposed` and `untouched` projects, collapses canonical repository copies, uses bounded manifest/source fingerprints, and freezes disjoint owner/content-independent acquisition and holdout partitions. Report `self_development_corpus_eligibility_20260831T052410743974Z.json` covers 4903 copies and 4321 unique projects with 644 untouched packaging-marker candidates. Acquisition and frozen holdout remain independently replenished at `3/3`; checks remain `7/7`. The separately justified pytest-plugin network fallback is now indexed as exposed evidence.

The token-aware archetype matcher supports `required_source_contains_all` for typed source contracts. Rule `owned_packaging_build_backend` requires owned PEP 517 hooks (`build_wheel`, `build_sdist`, `prepare_metadata_for_build_wheel`) plus a backend source marker. This outranks incidental config-parser vocabulary without treating a consumer-only `pyproject.toml` declaration as implementation evidence. Real verification classified `setuptools`, `flit`, and `poetry-core` as `framework_plugin_build/packaging_build_backend`; `setuptools` confidence is `0.79` and the consumer contrast remains outside the rule.

`ClassificationConsistencyEvidence` is computed independently from matcher output by bounded AST contract extraction. A final recognized classification that contradicts an owned contract creates `classification_contradiction`, whose only development route is research. Unknown or ambiguous recognition is deferred to the existing recognition lifecycle, and absent contracts are neutral. Prospective detection clusters this rule separately by contract subject and project type; source apply and promotion remain disabled.

Contract profile `owned_pytest_plugin` requires a `pytest11` manifest registration plus owned production fixture or lifecycle-hook evidence; registration may target a package module instead of a literal `plugin.py`. Project Analyzer intake includes bounded `.cfg`/`.ini` text and prioritizes `setup.cfg`/`plugin.py`; config samples and literal plugin modules receive a 5000-character cap while ordinary Python source remains at 1200. Frozen report `artifacts/pytest_plugin_holdout_20260831/holdout_report.json` binds three distinct owners and exact revisions: recognition/consistency and executable foundation acceptance pass `3/3`, role minima are `9.7/9.7/9.8`, and source changes are zero. Dependency-only and conftest-only projects remain outside the contract; downstream role promotion remains unproven.

Downstream calibration now includes one real historical pytest-plugin defect, not a derived transformation. `artifacts/field_trials/project_native_failure_intake_20260831T060005886998Z.json` repeats the original Faker-disabled regression failure twice on pre-fix revision `c9181c28607e990123ee480200ae2e684f58e7b6` and uniquely binds the leaf `TypeError` to `faker_seed`. `artifacts/project_development/project_development_20260831T060548304438Z.json` records the fail-closed result: no verified reducer, staged `unsupported_reducer_shape` research at `0.68`, no patch, no source changes, and no executor rerun. This is qualification and research evidence only; it does not raise Implementer, Tester, or Reviewer maturity.

The second historical defect is pytest-httpserver readiness cleanup. Intake `artifacts/field_trials/project_native_failure_intake_20260831T062053744758Z.json` binds two identical failures to `HTTPServer.start` through a unique state-transition slice over a test-local subclass. The chain preserves that failure-backed target across SpecWriter and routes execution-blocked network-risk to read-only research; `artifacts/project_development/project_development_20260831T062528187092Z.json` records the same staged `0.68` boundary and no mutation. Aggregate downstream evidence remains two qualified defects and zero autonomous transformations.

Pytest-socket provides the third independent defect. `artifacts/field_trials/project_native_failure_intake_20260831T071448820513Z.json` binds a repeated pickle reconstruction `TypeError` to `SocketConnectBlockedError.__init__` using unique runtime constructor identity. A same-project passing exception supplies non-promotional paired AST evidence. The staged profile reaches confidence `0.88` and evidence acceptance in `artifacts/project_development/project_development_20260831T072208003451Z.json`, but remains behind explicit human approval; execution, mutation, and KB promotion are false. Discovery breadth is three projects, transformation count is zero.

`SelfDevelopmentProspectiveCollectorReceipt` binds each new persisted development report to an incremental checkpoint by SHA-256; duplicate content does not rerun detection. Declarative derived signals include role handoff misalignment, human rejection and false validated-memory authority. `SelfDevelopmentL0CandidateAdmission` requires three independent projects, unseen holdout, no role regression, independent evaluator and proposal-bound reviewer decision. `SelfDevelopmentL0StagingTransaction` writes only under `artifacts/self_development/staged_candidates` or quarantine, never active KB, and can restore the exact pre-transaction state during rollback rehearsal. Fresh report `self_development_fresh_blind_trial_20260830T183308224089Z.json` reached `evidence_exhausted` with target coverage `3+3+3`, candidates `0` and checks `6/6`.

Exception-pickle active sync marker: `exception_pickle_active_application_20260903T065054426561Z.json` latest exact-target repair ledger accepted `astronomer__agents / astro-airflow-mcp/src/astro_airflow_mcp/adapters/base.py:NotFoundError.__init__` after exact promotions for Alembic range/head errors, JMComic base error, two Redis HTTP errors, Pact interaction verification, Discord command invoke and GraphQL WS response errors; latest intelligence `exception_pickle_blocker_intelligence_20260903T065159200878Z.json` uses latest-blocker frontier accounting and reports `applied=178`, ledger `blocked=414`, latest-blocker frontier `146`, readmission frontier `0`, next lane `import_dependency_isolation_candidate:25`; latest planner `exception_pickle_import_cluster_planner_20260903T065218069738Z.json` marks `unknown:attribute_base_metaclass_risk` as `not_batch_ready`; latest failed-probe analyzer `exception_pickle_failed_probe_analyzer_20260903T065213416880Z.json` recommends `constructor_state_contract_research`. The current cycle also closed the `graphql`, `overrides`, `platformdirs` and `pydantic_settings` one-target clusters; Chroma exposed and verified narrow `dir`/`version` materializer coverage plus decorator-factory stub replay. The current cycle also closed `volcengine` and `vonage_jwt`; Volcengine verified `missing_services` list samples, and Vonage verified a bounded response-object sample with `status_code`, `url`, `content`, `text` and `json()`. The current project-local cycle added `project_local_direct_file`, filters out attribute-base and target self-attribute-gap cases, scopes failed-probe evidence by profile/cluster, and promoted `langchain-ai__langchain / SSRFBlockedError`, `shy3130__tick-stock-panel / JobCancelledError`, and `astronomer__agents / NotFoundError`; stub path joins now use the platform temp directory and `endpoint` string samples are supported. Class-state audit `exception_pickle_class_state_contract_audit_20260903T070847705234Z.json` splits the 10 attribute-base/metaclass-risk cases into 7 `base_constructor_passthrough_contract`, 2 `attribute_base_state_contract`, and 1 `target_self_state_gap_research`; the next narrow lane is parent-constructor argument/state preservation research before replay. Source line-limit audit `source_line_limit_audit_20260903T230204065085Z.json` now passes across the scanned Python corpus: 1513 Python files checked, 0 files exceed the 400-line limit. The final cleanup kept runtime clean, split the two remaining tool probes behind stable facades, split the project-map domain-profile plugin tests, and moved oversized runtime test suites into helper-backed context-bounded test modules. This records the 400-line rule as enforced for runtime, tools, plugins and tests, not as optional refactoring guidance.
Hard maintainability is now part of the baseline. `ConfigDoctor` runs `source_line_limit_gate`; latest `source_line_limit_audit_20260903T230204065085Z.json` scanned 1513 authoritative Python files with zero files over 400 lines. `post_split_audit_20260903T230640450590Z.json` separately records 24 mechanical split facades and 55 helper-backed split tests, so future work can turn mechanical split boundaries into cohesive modules without weakening the hard line gate.
Derived-message sync marker: `exception_pickle_derived_message_audit_20260902T045536203364Z.json` reports 0 derived-message cases; source apply and KB promotion remain false.
Derived-import sync marker: `exception_pickle_derived_import_audit_20260902T045554559495Z.json` reports 0 derived import-isolation cases; source apply and KB promotion remain false.

### 12. Interpreter Authority and Narrow Certification

`config/interpreter_authority.json` defines the allowed engineering-stage transitions and bounded return policy. `runtime/interpreter_authority.py` emits a replayable `InterpreterDecisionTrace` containing a digest-bound input snapshot, evidence set, all considered alternatives, selected rule and prior-trace identity. Configuration, knowledge and registry records are valid decision authority; raw LLM output is not. A malformed transition, changed goal or target, expanded scope, invalid prior trace or exhausted return budget produces `controlled_stop`.

Role feedback uses `RoleRecoveryContract`. `needs_rework` and `research_more` are explicit return outcomes, preserve the original target, may only reduce scope and never authorize execution or an automatic retry. Role-chain summaries count both bounded and blocked returns in addition to handoff loss and arbitration metrics.

`NarrowTypeCertification` is distinct from role maturity. The current lane contains `cli_local_tool` and `library_pure_transform`; all Project Analyzer, Architect, Spec Writer, Implementer, Tester and Reviewer cells must reach `9.7+`, remain promotion eligible and resolve an immutable holdout receipt proving lineage disjointness, role-chain continuity, no role regression and zero generated function stubs. A missing or weak cell yields `evidence_required`, not a rounded-up score. Broad project types stay deferred.

`SelfDevelopmentExperiment` is the mandatory baseline/candidate authority for L0/L1 staging. It requires an independently evaluated ledger receipt, a positive target delta, no regression on any measured dimension and zero generated function stubs. The existing L0 lifecycle no longer accepts three self-reported booleans as sufficient verification; it requires the verified experiment before reviewer staging and still forbids active KB writes and automatic promotion.

`SelfDevelopmentExperimentQueue` closes the authority gap between prospective detection and that experiment. It accepts only digest-valid `NarrowTypeCertification` evidence whose underlying holdout receipt still verifies, then filters detected L0/L1 candidates to the certified project strata and assigns an explicit target metric and evidence contract. Receipt `sha256:614904cb8cf361533519162f348da9c7bf57d0c828960432ec30e8384a42bc15` records the first run: `waiting_for_candidate`, `5/5` checks passed, zero ready candidates. The framework/plugin watchlist cannot enter this queue because `framework_plugin_build` remains outside the certified narrow lane.

`CorpusEvaluationPlan` consumes normalized inventory from the existing local corpus, excludes previously exposed projects, collapses identical content and freezes content/lineage-disjoint holdout before acquisition. External acquisition is allowed only when the report contains a measured shortage. Unknown marker clusters remain `collect_more_cases` until independent project, lineage, digest and marker thresholds pass, and then become research candidates rather than promoted archetypes.

`CognitiveOSEvolutionAuthorityReport` combines interpreter verification, role-chain continuity, corpus split status, narrow certification and a bounded self-development experiment without converting readiness into a maturity claim. Web UI remains explicitly deferred until these authority contracts and the underlying architecture are stable.

Current evidence audit (2026-09-04) replaces historical blind inputs with four fresh transformation reports drawn from two independent local corpus lineages. The resulting matrix has all 12 required `cli_local_tool` and `library_pure_transform` cells promotion eligible at `9.7+`, with at least two blind projects per cell, disjoint acquisition/holdout lineages and no evidence gaps. The role pipeline passes `8/8`, minimum interaction is `1.0`, and handoff loss is zero. `GeneratedFunctionStubAudit` is SHA-256-bound to every contributing blind report and records four admissions, zero generated stubs and zero parse failures. Seven verified input receipts preserve immutable copies of the evaluation, role pipeline, stub audit and all four blind reports. Immutable holdout receipt `3a8e427d7763e61831f23f5bedb85f44da2a80c15c7c0ebddc554dcce73f6f1d` and certificate receipt `ca0bdda72697373d304b9a28923728612da25a58e09b4ad10e07c8f57f6bd657` verify successfully. `NarrowTypeCertification` is therefore `certified`; its scope does not include broad strata, automatic source apply or Web UI.
