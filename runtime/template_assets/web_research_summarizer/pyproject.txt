[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "web_research_summarizer"
version = "0.1.0"
requires-python = ">=3.10"

[project.scripts]
web-research-summarizer = "web_research_summarizer.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
